'=======================================================================================
'Unifloc 7.59  2026.02                                 khabibullinra@gmail.com
'Petroleum engineering calculations modules (macroses)
'2000 - 2025
'
'=======================================================================================
'функции для расчета гидродинамических исследований
'
Option Explicit
' данные по границе
' для радиальной модели
Public Type T_BOUNDARY_RADIAL
    shape As Integer
    d_m As Double
    d1_m  As Double
    d2_m  As Double
    d_band_m  As Double
    d_band_fr  As Double
    w_m  As Double
    l_m  As Double
    w_fr  As Double
    l_fr  As Double
    r_circle_m  As Double
    d_well_m  As Double
    const_pressure As Boolean
End Type
Public Type T_RESERVOIR
    k_mD As Double
    porosity As Double
    h_m As Double
    mu_cP As Double
    b_m3m3 As Double
    ct_1atm As Double
    p_init_atma As Double
End Type
Public Type T_WELL
    rw_m As Double    ' радиус скважины
    skin As Double    ' скин-фактор
    cs_1atm As Double ' влияние ствола скважины
    model_line_source As Boolean  ' флаг решения линейного стока
End Type
'description_to_manual      - для автогенерации
' расчет изменения забойного давления после запуска скважины
' с постоянным дебитом (terminal rate solution)
Function transient_pwf_radial_atma_(ByVal t_hr As Double, _
                                   ByVal q_liq_sm3day As Double, _
                          Optional ByVal pi_atma As Double = 250, _
                          Optional ByVal skin As Double = 0, _
                          Optional ByVal cs_1atm As Double = 0, _
                          Optional ByVal r_m As Double = 0.1, _
                          Optional ByVal rw_m As Double = 0.1, _
                          Optional ByVal k_mD As Double = 100, _
                          Optional ByVal h_m As Double = 10, _
                          Optional ByVal porosity As Double = 0.2, _
                          Optional ByVal mu_cP As Double = 1, _
                          Optional ByVal b_m3m3 As Double = 1.2, _
                          Optional ByVal ct_1atm As Double = 0.00001, _
                          Optional ByVal model As Integer = 0) As Double
' t_hr        - время для которого проводится расчет, час
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' pi_atma     - начальное пластовое давление, атма
' skin        - скин - фактор, может быть отрицательным
' cs_1atm     - коэффициент влияния ствола скважины, 1/атм
' r_m         - расстояние от скважины для которого проводится расчет, м
' rw_m        - радиус скважины, м
' k_mD        - проницаемость пласта, мД
' h_m         - толщина пласта, м
' porosity    - пористость
' mu_cP       - вязкость флюида в пласте, сП
' b_m3m3      - объемный коэффициент нефти, м3/м3
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' model      - модель проведения расчета. 0 - модель линейного стока Ei
'              1 - модель линейного стока через преобразование Стефеста
'              2 - конечный радиус скважины
'              3 - линейный сток со скином и послепритоком
'              4 - конечный радиус скважины со скином и послепритоком
' результат -  давление pwf
'description_end
    Dim td As Double, cd As Double, rd As Double
    Dim pd As Double
    Dim delta_p_atm As Double
    
    cd = 0.159 / h_m / porosity / ct_1atm / (rw_m * rw_m) * cs_1atm
    
    cd = trdef_cd_from_cs(cs_1atm, rw_m, h_m, porosity, ct_1atm)
    If skin < 0 Then
        rw_m = rw_m * Exp(-skin)
        If r_m < rw_m Then r_m = rw_m
        skin = 0
    End If
    
    td = trdef_td_from_t(t_hr, rw_m, k_mD, porosity, mu_cP, ct_1atm)
    
    rd = r_m / rw_m
    
    pd = transient_pd_radial(td, cd, skin, rd, model)
    
    delta_p_atm = trdef_p_from_pd_atma(pd, q_liq_sm3day, pi_atma, k_mD, h_m, mu_cP, b_m3m3)
    
    transient_pwf_radial_atma_ = delta_p_atm
    
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного коэффициента влияния ствола скважины (определение)
Function trdef_cd_from_cs( _
                      ByVal cs_1atm, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal h_m As Double = 10, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' cs_1atm     - коэффициент влияния ствола скважины, 1/атм
' rw_m        - радиус скважины, м
' h_m         - толщина пласта, м
' porosity    - пористость
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - безразмерный коэффициент влияния ствола скважины  cd
'description_end
    
    trdef_cd_from_cs = 0.159 / h_m / porosity / ct_1atm / (rw_m * rw_m) * cs_1atm
    
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного коэффициента влияния ствола скважины (определение)
Function trdef_cd_arr_from_cs( _
                      ByVal cs_1atm, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal h_m As Double = 10, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' cs_1atm     - коэффициент влияния ствола скважины, число или json список 1/атм
' rw_m        - радиус скважины, м
' h_m         - толщина пласта, м
' porosity    - пористость
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - безразмерный коэффициент влияния ствола скважины  cd
'description_end
    
    Dim cs_arr
    Dim i As Integer
    
    cs_arr = parse_list(cs_1atm)
    If UBound(cs_arr) = LBound(cs_arr) Then
        trdef_cd_arr_from_cs = 0.159 / h_m / porosity / ct_1atm / (rw_m * rw_m) * cs_1atm
        Exit Function
    Else
        ReDim res(LBound(cs_arr) To UBound(cs_arr)) As Double
        For i = LBound(cs_arr) To UBound(cs_arr)
            res(i) = 0.159 / h_m / porosity / ct_1atm / (rw_m * rw_m) * cs_arr(i)
        Next i
    End If
    trdef_cd_arr_from_cs = ConvertToJson(res)
End Function
'description_to_manual      - для автогенерации
' расчет коэффициента влияния ствола скважины (определение)
Function trdef_cs_from_cd_1atm(ByVal cd, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal h_m As Double = 10, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' cd          - коэффициент влияния ствола скважины, безразмерный
' rw_m        - радиус скважины, м
' h_m         - толщина пласта, м
' porosity    - пористость
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - коэффициент влияния ствола скважины  cs
'description_end
    
    trdef_cs_from_cd_1atm = 1 / 0.159 * h_m * porosity * ct_1atm * (rw_m * rw_m) * cd
       
End Function
'description_to_manual      - для автогенерации
' расчет коэффициента влияния ствола скважины (определение)
Function trdef_cs_arr_from_cd_1atm(ByVal cd, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal h_m As Double = 10, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' cd          - коэффициент влияния ствола скважины, безразмерный, число или json список
' rw_m        - радиус скважины, м
' h_m         - толщина пласта, м
' porosity    - пористость
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - коэффициент влияния ствола скважины  cs
'description_end
    
    Dim cd_arr
    Dim i As Integer
    
    cd_arr = parse_list(cd)
    If UBound(cd_arr) = LBound(cd_arr) Then
        trdef_cs_arr_from_cd_1atm = 1 / 0.159 * h_m * porosity * ct_1atm * (rw_m * rw_m) * cd
        Exit Function
    Else
        ReDim res(LBound(cd_arr) To UBound(cd_arr)) As Double
        For i = LBound(cd_arr) To UBound(cd_arr)
            res(i) = 1 / 0.159 * h_m * porosity * ct_1atm * (rw_m * rw_m) * cd_arr(i)
        Next i
    End If
    trdef_cs_arr_from_cd_1atm = ConvertToJson(res)
       
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного времени (определение)
Function trdef_td_from_t(ByVal t_hr, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal k_mD As Double = 100, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal mu_cP As Double = 1, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' t_hr        - время для которого проводится расчет, час
' rw_m        - радиус скважины, м
' k_mD        - проницаемость пласта, мД
' porosity    - пористость
' mu_cP       - вязкость флюида в пласте, сП
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - безразмерное время td
'description_end
    
    trdef_td_from_t = 0.00036 * k_mD / porosity / mu_cP / ct_1atm / (rw_m * rw_m) * t_hr
    
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного времени (определение)
Function trdef_td_arr_from_t(ByVal t_hr, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal k_mD As Double = 100, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal mu_cP As Double = 1, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' t_hr        - время для которого проводится расчет, час
' rw_m        - радиус скважины, м
' k_mD        - проницаемость пласта, мД
' porosity    - пористость
' mu_cP       - вязкость флюида в пласте, сП
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - безразмерное время td
'description_end
    
    Dim t_arr
    Dim i As Integer
    
    t_arr = parse_list(t_hr)
    If UBound(t_arr) = LBound(t_arr) Then
        trdef_td_arr_from_t = 0.00036 * k_mD / porosity / mu_cP / ct_1atm / (rw_m * rw_m) * t_hr
        Exit Function
    Else
        ReDim res(LBound(t_arr) To UBound(t_arr)) As Double
        For i = LBound(t_arr) To UBound(t_arr)
            res(i) = 0.00036 * k_mD / porosity / mu_cP / ct_1atm / (rw_m * rw_m) * t_arr(i)
        Next i
    End If
    trdef_td_arr_from_t = ConvertToJson(res)
End Function
'description_to_manual      - для автогенерации
' расчет времени по безразмерному времени (определение)
Function trdef_t_from_td_hr(ByVal td, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal k_mD As Double = 100, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal mu_cP As Double = 1, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' td          - время для которого проводится расчет, безразмерное
' rw_m        - радиус скважины, м
' k_mD        - проницаемость пласта, мД
' porosity    - пористость
' mu_cP       - вязкость флюида в пласте, сП
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - время t
'description_end
    
    trdef_t_from_td_hr = 1 / 0.00036 / k_mD * porosity * mu_cP * ct_1atm * (rw_m * rw_m) * td
       
End Function
'description_to_manual      - для автогенерации
' расчет времени по безразмерному времени (определение)
Function trdef_t_arr_from_td_hr(ByVal td, _
             Optional ByVal rw_m As Double = 0.1, _
             Optional ByVal k_mD As Double = 100, _
             Optional ByVal porosity As Double = 0.2, _
             Optional ByVal mu_cP As Double = 1, _
             Optional ByVal ct_1atm As Double = 0.00001 _
             )
' td          - время для которого проводится расчет, безразмерное
' rw_m        - радиус скважины, м
' k_mD        - проницаемость пласта, мД
' porosity    - пористость
' mu_cP       - вязкость флюида в пласте, сП
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' результат   - время t
'description_end
    
    Dim td_arr
    Dim i As Integer
    td_arr = parse_list(td)
    If UBound(td_arr) = LBound(td_arr) Then
        trdef_t_arr_from_td_hr = 1 / 0.00036 / k_mD * porosity * mu_cP * ct_1atm * (rw_m * rw_m) * td
        Exit Function
    Else
        ReDim res(LBound(td_arr) To UBound(td_arr)) As Double
        For i = LBound(td_arr) To UBound(td_arr)
            res(i) = 1 / 0.00036 / k_mD * porosity * mu_cP * ct_1atm * (rw_m * rw_m) * td_arr(i)
        Next i
    End If
    trdef_t_arr_from_td_hr = ConvertToJson(res)
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного давления (определение)
Function trdef_pd_from_p(ByVal p_wf_atma, _
                          ByVal q_liq_sm3day As Double, _
                 Optional ByVal pi_atma As Double = 250, _
                 Optional ByVal k_mD As Double = 100, _
                 Optional ByVal h_m As Double = 10, _
                 Optional ByVal mu_cP As Double = 1, _
                 Optional ByVal b_m3m3 As Double = 1.2 _
             )
' p_wf_atma    - забойное давление, атма
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' pi_atma     - начальное пластовое давление, атма
' k_mD        - проницаемость пласта, мД
' h_m         - толщина пласта, м
' mu_cP       - вязкость флюида в пласте, сП
' b_m3m3      - объемный коэффициент нефти, м3/м3
' результат   - безразмерное время td
'description_end
    
    trdef_pd_from_p = k_mD * h_m / 18.42 / q_liq_sm3day / mu_cP / b_m3m3 * (pi_atma - p_wf_atma)
    
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного давления (определение)
Function trdef_pd_arr_from_p(ByVal p_wf_atma, _
                          ByVal q_liq_sm3day As Double, _
                 Optional ByVal pi_atma As Double = 250, _
                 Optional ByVal k_mD As Double = 100, _
                 Optional ByVal h_m As Double = 10, _
                 Optional ByVal mu_cP As Double = 1, _
                 Optional ByVal b_m3m3 As Double = 1.2 _
             )
' p_wf_atma    - забойное давление, атма
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' pi_atma     - начальное пластовое давление, атма
' k_mD        - проницаемость пласта, мД
' h_m         - толщина пласта, м
' mu_cP       - вязкость флюида в пласте, сП
' b_m3m3      - объемный коэффициент нефти, м3/м3
' результат   - безразмерное время td
'description_end
    
    Dim p_arr
    Dim i As Integer
    p_arr = parse_list(p_wf_atma)
    If UBound(p_arr) = LBound(p_arr) Then
        trdef_pd_arr_from_p = k_mD * h_m / 18.42 / q_liq_sm3day / mu_cP / b_m3m3 * (pi_atma - p_wf_atma)
        Exit Function
    Else
        ReDim res(LBound(p_arr) To UBound(p_arr)) As Double
        For i = LBound(p_arr) To UBound(p_arr)
            res(i) = k_mD * h_m / 18.42 / q_liq_sm3day / mu_cP / b_m3m3 * (pi_atma - p_arr(i))
        Next i
    End If
    trdef_pd_arr_from_p = ConvertToJson(res)
    
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного давления (определение)
Function trdef_p_from_pd_atma(ByVal pd, _
                          ByVal q_liq_sm3day As Double, _
                 Optional ByVal pi_atma As Double = 250, _
                 Optional ByVal k_mD As Double = 100, _
                 Optional ByVal h_m As Double = 10, _
                 Optional ByVal mu_cP As Double = 1, _
                 Optional ByVal b_m3m3 As Double = 1.2 _
             )
' p_wf_atma    - забойное давление, атма
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' pi_atma     - начальное пластовое давление, атма
' k_mD        - проницаемость пласта, мД
' h_m         - толщина пласта, м
' mu_cP       - вязкость флюида в пласте, сП
' b_m3m3      - объемный коэффициент нефти, м3/м3
' результат   - безразмерное время td
'description_end
    
    trdef_p_from_pd_atma = pi_atma - 18.42 / k_mD / h_m * q_liq_sm3day * mu_cP * b_m3m3 * pd
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного давления (определение)
Function trdef_p_arr_from_pd_atma(ByVal pd, _
                          ByVal q_liq_sm3day As Double, _
                 Optional ByVal pi_atma As Double = 250, _
                 Optional ByVal k_mD As Double = 100, _
                 Optional ByVal h_m As Double = 10, _
                 Optional ByVal mu_cP As Double = 1, _
                 Optional ByVal b_m3m3 As Double = 1.2 _
             )
' p_wf_atma    - забойное давление, атма
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' pi_atma     - начальное пластовое давление, атма
' k_mD        - проницаемость пласта, мД
' h_m         - толщина пласта, м
' mu_cP       - вязкость флюида в пласте, сП
' b_m3m3      - объемный коэффициент нефти, м3/м3
' результат   - безразмерное время td
'description_end
    
    Dim pd_arr
    Dim i As Integer
    pd_arr = parse_list(pd)
    If UBound(pd_arr) = LBound(pd_arr) Then
        trdef_p_arr_from_pd_atma = pi_atma - 18.42 / k_mD / h_m * q_liq_sm3day * mu_cP * b_m3m3 * pd
        Exit Function
    Else
        ReDim res(LBound(pd_arr) To UBound(pd_arr)) As Double
        For i = LBound(pd_arr) To UBound(pd_arr)
            res(i) = pi_atma - 18.42 / k_mD / h_m * q_liq_sm3day * mu_cP * b_m3m3 * pd_arr(i)
        Next i
    End If
    trdef_p_arr_from_pd_atma = ConvertToJson(res)
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного давления (определение)
Function trdef_q_from_qd_sm3day(ByVal qd, _
                           Optional ByVal q_ref_sm3day As Double = 1 _
                                   )
' qd    - безразмерный дебит скважины
' q_ref_sm3day - дебит обезразмеривания, м3/сут в стандартных условиях
' результат   - безразмерное время td
'description_end
    
    trdef_q_from_qd_sm3day = qd * q_ref_sm3day / 2 / const_Pi
End Function
'description_to_manual      - для автогенерации
' расчет безразмерного давления (определение)
Function trdef_qd_from_q(ByVal q, _
                    Optional ByVal q_ref_sm3day As Double = 1 _
                             )
' qd    - безразмерный дебит скважины
' q_ref_sm3day - дебит обезразмеривания, м3/сут в стандартных условиях
' результат   - безразмерное время td
'description_end
    
    trdef_qd_from_q = q / q_ref_sm3day * 2 * const_Pi
End Function
Function tr_pwf_radial_atma(t_hr As Double, _
                              q_liq_sm3day As Double, _
                              well_json As String, _
                              res_json As String, _
                     Optional boundary_json As String) As Double
    Dim well As T_WELL
    Dim res As T_RESERVOIR
    Dim boundary As T_BOUNDARY_RADIAL
    
    well = tr_convert_json_well(well_json)
    res = tr_convert_json_reservoir(res_json)
    boundary = tr_convert_json_boundary(boundary_json)
     
    tr_pwf_radial_atma = tr__pwf_radial_atma_(t_hr, q_liq_sm3day, well, res, boundary)
    
End Function
Function tr__pwf_radial_atma_(t_hr As Double, _
                              q_liq_sm3day As Double, _
                              well As T_WELL, _
                              res As T_RESERVOIR, _
                              boundary As T_BOUNDARY_RADIAL) As Double
                     
' t_hr        - время для которого проводится расчет, час
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' well  - параметры скважины
' res - параметры пласта
' boundary - параметры границ
' результат -  давление pwf
'description_end
    Dim td As Double, cd As Double ', rd As Double
    Dim pd As Double
    Dim delta_p_atm As Double
    Dim wellModel As Integer
    Dim boundaryType As Integer
    Dim skin As Double
    Dim rw_eff_m As Double
    
    If well.model_line_source Then
        wellModel = 1
    Else
        wellModel = 2
    End If
    
    If boundary.const_pressure Then
        boundaryType = 2
    Else
        boundaryType = 1
    End If
    If well.skin < 0 Then
        skin = 0
        rw_eff_m = well.rw_m * Exp(-well.skin)
    Else
        skin = well.skin
        rw_eff_m = well.rw_m
    End If
    
    
    cd = trdef_cd_from_cs(well.cs_1atm, rw_eff_m, res.h_m, res.porosity, res.ct_1atm)
    td = trdef_td_from_t(t_hr, rw_eff_m, res.k_mD, res.porosity, res.mu_cP, res.ct_1atm)
    pd = PD_QD_QTD(1, td, cd, skin, _
                   wellModel, boundary.shape, boundaryType, _
                   boundary.d_m / rw_eff_m, _
                   boundary.d1_m / rw_eff_m, _
                   boundary.d2_m / rw_eff_m, _
                   boundary.d_band_m / rw_eff_m, _
                   boundary.d_band_fr, _
                   boundary.w_m / rw_eff_m, _
                   boundary.l_m / rw_eff_m, _
                   boundary.w_fr, _
                   boundary.l_fr, _
                   boundary.r_circle_m / rw_eff_m, _
                   boundary.d_well_m / rw_eff_m, _
                   rw_eff_m / rw_eff_m)
    
    
    tr__pwf_radial_atma_ = trdef_p_from_pd_atma(pd, q_liq_sm3day, res.p_init_atma, res.k_mD, res.h_m, res.mu_cP, res.b_m3m3)
    
End Function
'description_to_manual      - для автогенерации
' расчет изменения забойного давления после запуска скважины
' с постоянным дебитом (terminal rate solution)
Function transient_pwf_radial_kva_atma(ByVal t_hr As Double, _
                                       ByVal q_liq_sm3day As Double, _
                          Optional ByVal pi_atma As Double = 250, _
                          Optional ByVal skin As Double = 0, _
                          Optional ByVal cs_1atm As Double = 0, _
                          Optional ByVal r_m As Double = 0.1, _
                          Optional ByVal rw_m As Double = 0.1, _
                          Optional ByVal k_mD As Double = 100, _
                          Optional ByVal h_m As Double = 10, _
                          Optional ByVal porosity As Double = 0.2, _
                          Optional ByVal mu_cP As Double = 1, _
                          Optional ByVal b_m3m3 As Double = 1.2, _
                          Optional ByVal ct_1atm As Double = 0.00001, _
                       Optional ByVal wellModel = 2, _
                       Optional ByVal ReservoirShape = 1, _
                       Optional ByVal boundaryType = 1, _
                       Optional ByVal d_m = 100, _
                       Optional ByVal d1_m = 100, Optional ByVal d2_m = 100, Optional ByVal db_m = 500, _
                       Optional ByVal d_band_f = 0.5, _
                       Optional ByVal w_m = 500, _
                       Optional ByVal l_m = 500, _
                       Optional ByVal w_f = 0.5, _
                       Optional ByVal l_f = 0.5, _
                       Optional ByVal r_ext_m = 1000, _
                       Optional ByVal d_well_m = 500) As Double
' t_hr        - время для которого проводится расчет, час
' q_liq_sm3day - дебит запуска скважины, м3/сут в стандартных условиях
' pi_atma     - начальное пластовое давление, атма
' skin        - скин - фактор, может быть отрицательным
' cs_1atm     - коэффициент влияния ствола скважины, 1/атм
' r_m         - расстояние от скважины для которого проводится расчет, м
' rw_m        - радиус скважины, м
' k_mD        - проницаемость пласта, мД
' h_m         - толщина пласта, м
' porosity    - пористость
' mu_cP       - вязкость флюида в пласте, сП
' b_m3m3      - объемный коэффициент нефти, м3/м3
' ct_1atm     - общая сжимаемость системы в пласте, 1/атм
' model      - модель проведения расчета. 0 - модель линейного стока Ei
'              1 - модель линейного стока через преобразование Стефеста
'              2 - конечный радиус скважины
'              3 - линейный сток со скином и послепритоком
'              4 - конечный радиус скважины со скином и послепритоком
'Boundary parameters - different should be used for different reservoir shapes
' D_m  - distance to boundary, (in half plane reservoir)
' D1_m - distanse to 1st boundary, (in quater plane reservoir)
' D2_m - distanse to 2d boundary, (in quater plane reservoir)
' Db_d -  band width (for band reservoir)
' d_band_f - relative distanse from one side of band to well, fraction of band width (for band reservoir)
' W_m - rectangle width (for rectangular reservoir)
' l_m - rectangle length (for rectangular reservoir)
' w_f - relative distanse from long side of rectangle to well, fraction of rectangle width (for rectangular reservoir)
' l_f - relative distanse from short side of rectangle to well, fraction of rectangle length (for rectangular reservoir)
' r_ext_m - circle radius (for circular reservoir)
' d_well_m - well spacing (for 5 spot pattern)
' r_m - distance from center of wellbore
' результат -  давление pwf
'description_end
    Dim td As Double, cd As Double, rd As Double
    Dim pd As Double
    Dim delta_p_atm As Double
    
    cd = 0.159 / h_m / porosity / ct_1atm / (rw_m * rw_m) * cs_1atm
    
    cd = trdef_cd_from_cs(cs_1atm, rw_m, h_m, porosity, ct_1atm)
    
    td = trdef_td_from_t(t_hr, rw_m, k_mD, porosity, mu_cP, ct_1atm)
    
    rd = r_m / rw_m
    
    pd = PD_QD_QTD(1, td, cd, skin, _
                   wellModel, ReservoirShape, boundaryType, d_m / rw_m, d1_m / rw_m, d2_m / rw_m, db_m / rw_m, _
                   d_band_f, w_m / rw_m, l_m / rw_m, w_f, l_f, r_ext_m / rw_m, d_well_m / rw_m)
    
    
    transient_pwf_radial_kva_atma = trdef_p_from_pd_atma(pd, q_liq_sm3day, pi_atma, k_mD, h_m, mu_cP, b_m3m3)
    
End Function
' функции кодирования граничных условий для нестационарной модели
'
'
Public Function encode_transient_boundary_half_plane(d_m As Double, _
                                  Optional const_pressure As Boolean = False)
' d_m  - distance to boundary, (in half plane reservoir)
    Dim d As New Dictionary
    d.add "boundary", "half plane"
    d.add "shape", 2
    d.add "d_m", d_m
    d.add "const_pressure", const_pressure
    encode_transient_boundary_half_plane = ConvertToJson(d)
End Function
Public Function encode_transient_boundary_quarter_plane(d1_m As Double, _
                                                        d2_m As Double, _
                                     Optional const_pressure As Boolean = False)
' d1_m - distanse to 1st boundary, (in quater plane reservoir)
' d2_m - distanse to 2d boundary, (in quater plane reservoir)
    Dim d As New Dictionary
    d.add "boundary", "quarter plane"
    d.add "shape", 3
    d.add "d1_m", d1_m
    d.add "d2_m", d2_m
    d.add "const_pressure", const_pressure
    encode_transient_boundary_quarter_plane = ConvertToJson(d)
End Function
Public Function encode_transient_boundary_band(d_band_m As Double, _
                                               d_band_fr As Double, _
                                     Optional const_pressure As Boolean = False, _
                                     Optional m_band_reflections As Integer = 20)
' db_m - band width (for band reservoir)
' db_fr_m - relative distanse from one side of band to well, fraction of band width (for band reservoir)
    Dim d As New Dictionary
    d.add "boundary", "band"
    d.add "shape", 4
    d.add "d_band_m", d_band_m
    d.add "d_band_fr", d_band_fr
    d.add "const_pressure", const_pressure
    d.add "m_band_reflections", m_band_reflections
    encode_transient_boundary_band = ConvertToJson(d)
End Function
Public Function encode_transient_boundary_rectangle(w_m As Double, _
                                                    l_m As Double, _
                                                    w_fr As Double, _
                                                    l_fr As Double, _
                                     Optional const_pressure As Boolean = False, _
                                     Optional m_rect_reflections As Integer = 10)
' w_m - rectangle width (for rectangular reservoir)
' l_m - rectangle length (for rectangular reservoir)
' w_fr - relative distanse from long side of rectangle to well, fraction of rectangle width (for rectangular reservoir)
' l_fr - relative distanse from short side of rectangle to well, fraction of rectangle length (for rectangular reservoir)
    Dim d As New Dictionary
    d.add "boundary", "rectangle"
    d.add "shape", 5
    d.add "w_m", w_m
    d.add "l_m", l_m
    d.add "w_fr", w_fr
    d.add "l_fr", l_fr
    d.add "const_pressure", const_pressure
    d.add "m_rect_reflections", m_rect_reflections
    encode_transient_boundary_rectangle = ConvertToJson(d)
End Function
Public Function encode_transient_boundary_circle(r_circle_m As Double, _
                                  Optional const_pressure As Boolean = False)
' r_circle_m - circle radius (for circular reservoir)
    Dim d As New Dictionary
    d.add "boundary", "circle"
    d.add "shape", 6
    d.add "r_circle_m", r_circle_m
    d.add "const_pressure", const_pressure
    encode_transient_boundary_circle = ConvertToJson(d)
End Function
Public Function tr_convert_json_reservoir(json As String) As T_RESERVOIR
' преобразование json с данными по границе в объект для расчета
    Dim d As Dictionary
    Set d = ParseJson(json)
    Dim res As T_RESERVOIR
    
    With res
        .k_mD = 100
        .porosity = 0.2
        .h_m = 10
        .mu_cP = 1
        .b_m3m3 = 1.1
        .ct_1atm = 0.0001
        .p_init_atma = 200
    
        If d.Exists("k_mD") Then .k_mD = d("k_mD")
        If d.Exists("porosity") Then .porosity = d("porosity")
        If d.Exists("h_m") Then .h_m = d("h_m")
        If d.Exists("mu_cP") Then .mu_cP = d("mu_cP")
        If d.Exists("b_m3m3") Then .b_m3m3 = d("b_m3m3")
        If d.Exists("ct_1atm") Then .ct_1atm = d("ct_1atm")
        If d.Exists("p_init_atma") Then .p_init_atma = d("p_init_atma")
    End With
    
    tr_convert_json_reservoir = res
End Function
Public Function tr_convert_json_well(json As String) As T_WELL
' преобразование json с данными по границе в объект для расчета
    Dim d As Dictionary
    Set d = ParseJson(json)
    Dim wl As T_WELL
    
    With wl
        .rw_m = 100
        .skin = 0
        .cs_1atm = 0.0001
        .model_line_source = 0
    
        If d.Exists("rw_m") Then .rw_m = d("rw_m")
        If d.Exists("skin") Then .skin = d("skin")
        If d.Exists("cs_1atm") Then .cs_1atm = d("cs_1atm")
        If d.Exists("model_line_source") Then .model_line_source = d("model_line_source")
    End With
    
    tr_convert_json_well = wl
End Function
Public Function tr_convert_json_boundary(json As String) As T_BOUNDARY_RADIAL
' преобразование json с данными по границе в объект для расчета
    Dim d As Dictionary
    If Mid(json, 1, 1) = "{" Then
        Set d = ParseJson(json)
    Else
        Set d = New Dictionary
    End If
    Dim bnd As T_BOUNDARY_RADIAL
    
    With bnd
        .shape = 1
        .d_m = 100
        .d1_m = 100
        .d2_m = 100
        .d_band_m = 100
        .d_band_fr = 0.5
        .w_m = 100
        .l_m = 100
        .w_fr = 0.5
        .l_fr = 0.5
        .r_circle_m = 100
        .d_well_m = 100
        .const_pressure = False
    
        If d.Exists("shape") Then .shape = d("shape")
        If d.Exists("w_m") Then .w_m = d("w_m")
        If d.Exists("d1_m") Then .d1_m = d("d1_m")
        If d.Exists("d2_m") Then .d2_m = d("d2_m")
        If d.Exists("d_band_m") Then .w_m = d("d_band_m")
        If d.Exists("d_band_fr") Then .d_band_fr = d("d_band_fr")
        If d.Exists("w_m") Then .w_m = d("w_m")
        If d.Exists("l_m") Then .l_m = d("l_m")
        If d.Exists("w_fr") Then .w_fr = d("w_fr")
        If d.Exists("l_fr") Then .l_fr = d("l_fr")
        If d.Exists("r_circle_m") Then .r_circle_m = d("r_circle_m")
        If d.Exists("d_well_m") Then .d_well_m = d("d_well_m")
        If d.Exists("const_pressure") Then .const_pressure = d("const_pressure")
    End With
    
    tr_convert_json_boundary = bnd
End Function
Public Function encode_transient_well(Optional rw_m As Double = 0.1, _
                                      Optional skin As Double = 0, _
                                      Optional cs_1atm As Double = 0.0001, _
                                      Optional model_line_source As Boolean = False)
'    rw_m As Double    ' радиус скважины
'    skin As Double    ' скин-фактор
'    cs_1atm As Double ' влияние ствола скважины
'    model_line_source As Boolean  ' флаг решения линейного стока
'
    
    Dim d As New Dictionary
    d.add "well", "vertical"
    d.add "rw_m", rw_m
    d.add "skin", skin
    d.add "cs_1atm", cs_1atm
    d.add "model_line_source", 0
    encode_transient_well = ConvertToJson(d)
End Function
Public Function encode_transient_reservoir(Optional k_mD As Double = 10, _
                                           Optional porosity As Double = 0.2, _
                                           Optional h_m As Double = 10, _
                                           Optional mu_cP As Double = 1, _
                                           Optional b_m3m3 As Double = 1.1, _
                                           Optional ct_1atm As Double = 0.0001, _
                                           Optional p_init_atma As Double = 200)
'    k_mD As Double
'    porosity As Double
'    h_m As Double
'    mu_cP As Double
'    b_m3m3 As Double
'    ct_1atm As Double
'    p_init_atma As Double
    
    Dim d As New Dictionary
    d.add "reservoir", "basic"
    d.add "k_mD", k_mD
    d.add "porosity", porosity
    d.add "h_m", h_m
    d.add "mu_cP", mu_cP
    d.add "b_m3m3", b_m3m3
    d.add "ct_1atm", ct_1atm
    d.add "p_init_atma", p_init_atma
    
    encode_transient_reservoir = ConvertToJson(d)
End Function

