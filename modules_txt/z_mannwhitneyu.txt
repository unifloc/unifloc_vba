''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Copyright (c) 2007, Sergey Bochkanov (ALGLIB project).
'
'>>> SOURCE LICENSE >>>
'This program is free software; you can redistribute it and/or modify
'it under the terms of the GNU General Public License as published by
'the Free Software Foundation (www.fsf.org); either version 2 of the
'License, or (at your option) any later version.
'
'This program is distributed in the hope that it will be useful,
'but WITHOUT ANY WARRANTY; without even the implied warranty of
'MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
'GNU General Public License for more details.
'
'A copy of the GNU General Public License is available at
'http://www.fsf.org/licensing/licenses
'
'>>> END OF LICENSE >>>
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Routines
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Mann-Whitney U-test
'
'This test checks hypotheses about whether X  and  Y  are  samples  of  two
'continuous distributions of the same shape  and  same  median  or  whether
'their medians are different.
'
'The following tests are performed:
'    * two-tailed test (null hypothesis - the medians are equal)
'    * left-tailed test (null hypothesis - the median of the  first  sample
'      is greater than or equal to the median of the second sample)
'    * right-tailed test (null hypothesis - the median of the first  sample
'      is less than or equal to the median of the second sample).
'
'Requirements:
'    * the samples are independent
'    * X and Y are continuous distributions (or discrete distributions well-
'      approximating continuous distributions)
'    * distributions of X and Y have the  same  shape.  The  only  possible
'      difference is their position (i.e. the value of the median)
'    * the number of elements in each sample is not less than 5
'    * the scale of measurement should be ordinal, interval or ratio  (i.e.
'      the test could not be applied to nominal variables).
'
'The test is non-parametric and doesn't require distributions to be normal.
'
'Input parameters:
'    X   -   sample 1. Array whose index goes from 0 to N-1.
'    N   -   size of the sample. N>=5
'    Y   -   sample 2. Array whose index goes from 0 to M-1.
'    M   -   size of the sample. M>=5
'
'Output parameters:
'    BothTails   -   p-value for two-tailed test.
'                    If BothTails is less than the given significance level
'                    the null hypothesis is rejected.
'    LeftTail    -   p-value for left-tailed test.
'                    If LeftTail is less than the given significance level,
'                    the null hypothesis is rejected.
'    RightTail   -   p-value for right-tailed test.
'                    If RightTail is less than the given significance level
'                    the null hypothesis is rejected.
'
'To calculate p-values, special approximation is used. This method lets  us
'calculate p-values with satisfactory  accuracy  in  interval  [0.0001, 1].
'There is no approximation outside the [0.0001, 1] interval. Therefore,  if
'the significance level outlies this interval, the test returns 0.0001.
'
'Relative precision of approximation of p-value:
'
'N          M          Max.err.   Rms.err.
'5..10      N..10      1.4e-02    6.0e-04
'5..10      N..100     2.2e-02    5.3e-06
'10..15     N..15      1.0e-02    3.2e-04
'10..15     N..100     1.0e-02    2.2e-05
'15..100    N..100     6.1e-03    2.7e-06
'
'For N,M>100 accuracy checks weren't put into  practice,  but  taking  into
'account characteristics of asymptotic approximation used, precision should
'not be sharply different from the values for interval [5, 100].
'
'  -- ALGLIB --
'     Copyright 09.04.2007 by Bochkanov Sergey
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Public Sub MannWhitneyUTest(ByRef X() As Double, _
         ByVal N As Long, _
         ByRef Y() As Double, _
         ByVal M As Long, _
         ByRef BothTails As Double, _
         ByRef LeftTail As Double, _
         ByRef RightTail As Double)
    Dim i As Long
    Dim J As Long
    Dim K As Long
    Dim T As Long
    Dim Tmp As Double
    Dim TmpI As Long
    Dim NS As Long
    Dim R() As Double
    Dim C() As Long
    Dim U As Double
    Dim P As Double
    Dim MP As Double
    Dim S As Double
    Dim Sigma As Double
    Dim Mu As Double
    Dim TieCount As Long
    Dim TieSize() As Long
    
    '
    ' Prepare
    '
    If N <= 4# Or M <= 4# Then
        BothTails = 1#
        LeftTail = 1#
        RightTail = 1#
        Exit Sub
    End If
    NS = N + M
    ReDim R(0# To NS - 1#)
    ReDim C(0# To NS - 1#)
    For i = 0# To N - 1# Step 1
        R(i) = X(i)
        C(i) = 0#
    Next i
    For i = 0# To M - 1# Step 1
        R(N + i) = Y(i)
        C(N + i) = 1#
    Next i
    
    '
    ' sort {R, C}
    '
    If NS <> 1# Then
        i = 2#
        Do
            T = i
            Do While T <> 1#
                K = T \ 2#
                If R(K - 1#) >= R(T - 1#) Then
                    T = 1#
                Else
                    Tmp = R(K - 1#)
                    R(K - 1#) = R(T - 1#)
                    R(T - 1#) = Tmp
                    TmpI = C(K - 1#)
                    C(K - 1#) = C(T - 1#)
                    C(T - 1#) = TmpI
                    T = K
                End If
            Loop
            i = i + 1#
        Loop Until Not i <= NS
        i = NS - 1#
        Do
            Tmp = R(i)
            R(i) = R(0#)
            R(0#) = Tmp
            TmpI = C(i)
            C(i) = C(0#)
            C(0#) = TmpI
            T = 1#
            Do While T <> 0#
                K = 2# * T
                If K > i Then
                    T = 0#
                Else
                    If K < i Then
                        If R(K) > R(K - 1#) Then
                            K = K + 1#
                        End If
                    End If
                    If R(T - 1#) >= R(K - 1#) Then
                        T = 0#
                    Else
                        Tmp = R(K - 1#)
                        R(K - 1#) = R(T - 1#)
                        R(T - 1#) = Tmp
                        TmpI = C(K - 1#)
                        C(K - 1#) = C(T - 1#)
                        C(T - 1#) = TmpI
                        T = K
                    End If
                End If
            Loop
            i = i - 1#
        Loop Until Not i >= 1#
    End If
    
    '
    ' compute tied ranks
    '
    i = 0#
    TieCount = 0#
    ReDim TieSize(0# To NS - 1#)
    Do While i <= NS - 1#
        J = i + 1#
        Do While J <= NS - 1#
            If R(J) <> R(i) Then
                Exit Do
            End If
            J = J + 1#
        Loop
        For K = i To J - 1# Step 1
            R(K) = 1# + (i + J - 1#) / 2#
        Next K
        TieSize(TieCount) = J - i
        TieCount = TieCount + 1#
        i = J
    Loop
    
    '
    ' Compute U
    '
    U = 0#
    For i = 0# To NS - 1# Step 1
        If C(i) = 0# Then
            U = U + R(i)
        End If
    Next i
    U = N * M + N * (N + 1#) \ 2# - U
    
    '
    ' Result
    '
    Mu = N * M / 2#
    Tmp = NS * (Square(NS) - 1#) / 12#
    For i = 0# To TieCount - 1# Step 1
        Tmp = Tmp - TieSize(i) * (Square(TieSize(i)) - 1#) / 12#
    Next i
    Sigma = Sqr(M * N / NS / (NS - 1#) * Tmp)
    S = (U - Mu) / Sigma
    If S <= 0# Then
        P = Exp(USigma(-((U - Mu) / Sigma), N, M))
        MP = 1# - Exp(USigma(-((U - 1# - Mu) / Sigma), N, M))
    Else
        MP = Exp(USigma((U - Mu) / Sigma, N, M))
        P = 1# - Exp(USigma((U + 1# - Mu) / Sigma, N, M))
    End If
    BothTails = MaxReal(2# * MinReal(P, MP), 0.0001)
    LeftTail = MaxReal(MP, 0.0001)
    RightTail = MaxReal(P, 0.0001)
End Sub
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Sequential Chebyshev interpolation.
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Sub UCheb(ByVal X As Double, _
         ByVal C As Double, _
         ByRef TJ As Double, _
         ByRef TJ1 As Double, _
         ByRef R As Double)
    Dim T As Double
    R = R + C * TJ
    T = 2# * X * TJ1 - TJ
    TJ = TJ1
    TJ1 = T
End Sub
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Three-point polynomial interpolation.
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UNInterpolate(ByVal P1 As Double, _
         ByVal P2 As Double, _
         ByVal P3 As Double, _
         ByVal N As Long) As Double
    Dim Result As Double
    Dim T1 As Double
    Dim T2 As Double
    Dim T3 As Double
    Dim T As Double
    Dim P12 As Double
    Dim P23 As Double
    T1 = 1# / 15#
    T2 = 1# / 30#
    T3 = 1# / 100#
    T = 1# / N
    P12 = ((T - T2) * P1 + (T1 - T) * P2) / (T1 - T2)
    P23 = ((T - T3) * P2 + (T2 - T) * P3) / (T2 - T3)
    Result = ((T - T3) * P12 + (T1 - T) * P23) / (T1 - T3)
    UNInterpolate = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(0, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma000(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-0.676984, -0.6837, -0.689873, N2)
    P2 = UNInterpolate(-0.6837, -0.687311, -0.690957, N2)
    P3 = UNInterpolate(-0.689873, -0.690957, -0.692175, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma000 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(0.75, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma075(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-1.445, -1.45906, -1.47063, N2)
    P2 = UNInterpolate(-1.45906, -1.46856, -1.47644, N2)
    P3 = UNInterpolate(-1.47063, -1.47644, -1.481, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma075 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(1.5, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma150(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-2.6538, -2.67352, -2.69011, N2)
    P2 = UNInterpolate(-2.67352, -2.68591, -2.69659, N2)
    P3 = UNInterpolate(-2.69011, -2.69659, -2.70192, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma150 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(2.25, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma225(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-4.41465, -4.4226, -4.43702, N2)
    P2 = UNInterpolate(-4.4226, -4.41639, -4.41928, N2)
    P3 = UNInterpolate(-4.43702, -4.41928, -4.4103, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma225 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(3.0, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma300(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-6.89839, -6.83477, -6.8234, N2)
    P2 = UNInterpolate(-6.83477, -6.74559, -6.71117, N2)
    P3 = UNInterpolate(-6.8234, -6.71117, -6.64929, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma300 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(3.33, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma333(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-8.31272, -8.17096, -8.13125, N2)
    P2 = UNInterpolate(-8.17096, -8.00156, -7.93245, N2)
    P3 = UNInterpolate(-8.13125, -7.93245, -7.82502, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma333 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(3.66, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma367(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-9.98837, -9.70844, -9.62087, N2)
    P2 = UNInterpolate(-9.70844, -9.41156, -9.28998, N2)
    P3 = UNInterpolate(-9.62087, -9.28998, -9.11686, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma367 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(4.0, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma400(ByVal N1 As Long, ByVal N2 As Long) As Double
    Dim Result As Double
    Dim P1 As Double
    Dim P2 As Double
    Dim P3 As Double
    P1 = UNInterpolate(-12.025, -11.4911, -11.3231, N2)
    P2 = UNInterpolate(-11.4911, -10.9927, -10.7937, N2)
    P3 = UNInterpolate(-11.3231, -10.7937, -10.5285, N2)
    Result = UNInterpolate(P1, P2, P3, N1)
    USigma400 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 5)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N5(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 2.611165 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -2.596264, TJ, TJ1, Result)
    Call UCheb(X, -2.412086, TJ, TJ1, Result)
    Call UCheb(X, -0.4858542, TJ, TJ1, Result)
    Call UCheb(X, -0.05614282, TJ, TJ1, Result)
    Call UCheb(X, 0.003372686, TJ, TJ1, Result)
    Call UCheb(X, 0.008524731, TJ, TJ1, Result)
    Call UCheb(X, 0.004435331, TJ, TJ1, Result)
    Call UCheb(X, 0.001284665, TJ, TJ1, Result)
    Call UCheb(X, 0.004184141, TJ, TJ1, Result)
    Call UCheb(X, 0.00529836, TJ, TJ1, Result)
    Call UCheb(X, 0.0007447272, TJ, TJ1, Result)
    Call UCheb(X, -0.003938769, TJ, TJ1, Result)
    Call UCheb(X, -0.004276205, TJ, TJ1, Result)
    Call UCheb(X, -0.001138481, TJ, TJ1, Result)
    Call UCheb(X, 0.0008684625, TJ, TJ1, Result)
    Call UCheb(X, 0.001558104, TJ, TJ1, Result)
    UTblN5N5 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 6)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N6(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 2.738613 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -2.810459, TJ, TJ1, Result)
    Call UCheb(X, -2.684429, TJ, TJ1, Result)
    Call UCheb(X, -0.5712858, TJ, TJ1, Result)
    Call UCheb(X, -0.08009324, TJ, TJ1, Result)
    Call UCheb(X, -0.006644391, TJ, TJ1, Result)
    Call UCheb(X, 0.006034173, TJ, TJ1, Result)
    Call UCheb(X, 0.004953498, TJ, TJ1, Result)
    Call UCheb(X, 0.003279293, TJ, TJ1, Result)
    Call UCheb(X, 0.003563485, TJ, TJ1, Result)
    Call UCheb(X, 0.004971952, TJ, TJ1, Result)
    Call UCheb(X, 0.003506309, TJ, TJ1, Result)
    Call UCheb(X, -0.0001541406, TJ, TJ1, Result)
    Call UCheb(X, -0.003283205, TJ, TJ1, Result)
    Call UCheb(X, -0.003016347, TJ, TJ1, Result)
    Call UCheb(X, -0.001221626, TJ, TJ1, Result)
    Call UCheb(X, -0.001286752, TJ, TJ1, Result)
    UTblN5N6 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 7)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N7(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 2.841993 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -2.994677, TJ, TJ1, Result)
    Call UCheb(X, -2.923264, TJ, TJ1, Result)
    Call UCheb(X, -0.650619, TJ, TJ1, Result)
    Call UCheb(X, -0.105428, TJ, TJ1, Result)
    Call UCheb(X, -0.01794587, TJ, TJ1, Result)
    Call UCheb(X, 0.00172629, TJ, TJ1, Result)
    Call UCheb(X, 0.00453418, TJ, TJ1, Result)
    Call UCheb(X, 0.004517845, TJ, TJ1, Result)
    Call UCheb(X, 0.003904428, TJ, TJ1, Result)
    Call UCheb(X, 0.003882443, TJ, TJ1, Result)
    Call UCheb(X, 0.003482988, TJ, TJ1, Result)
    Call UCheb(X, 0.002114875, TJ, TJ1, Result)
    Call UCheb(X, -0.0001515082, TJ, TJ1, Result)
    Call UCheb(X, -0.001996056, TJ, TJ1, Result)
    Call UCheb(X, -0.002293581, TJ, TJ1, Result)
    Call UCheb(X, -0.002349444, TJ, TJ1, Result)
    UTblN5N7 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 8)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N8(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 2.9277 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.155727, TJ, TJ1, Result)
    Call UCheb(X, -3.135078, TJ, TJ1, Result)
    Call UCheb(X, -0.7247203, TJ, TJ1, Result)
    Call UCheb(X, -0.1309697, TJ, TJ1, Result)
    Call UCheb(X, -0.02993725, TJ, TJ1, Result)
    Call UCheb(X, -0.003567219, TJ, TJ1, Result)
    Call UCheb(X, 0.003383704, TJ, TJ1, Result)
    Call UCheb(X, 0.005002188, TJ, TJ1, Result)
    Call UCheb(X, 0.004487322, TJ, TJ1, Result)
    Call UCheb(X, 0.003443899, TJ, TJ1, Result)
    Call UCheb(X, 0.00268827, TJ, TJ1, Result)
    Call UCheb(X, 0.002600339, TJ, TJ1, Result)
    Call UCheb(X, 0.001874948, TJ, TJ1, Result)
    Call UCheb(X, 0.0001811593, TJ, TJ1, Result)
    Call UCheb(X, -0.001072353, TJ, TJ1, Result)
    Call UCheb(X, -0.002659457, TJ, TJ1, Result)
    UTblN5N8 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 9)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N9(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3# - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.298162, TJ, TJ1, Result)
    Call UCheb(X, -3.325016, TJ, TJ1, Result)
    Call UCheb(X, -0.7939852, TJ, TJ1, Result)
    Call UCheb(X, -0.1563029, TJ, TJ1, Result)
    Call UCheb(X, -0.04222652, TJ, TJ1, Result)
    Call UCheb(X, -0.0091952, TJ, TJ1, Result)
    Call UCheb(X, 0.001445665, TJ, TJ1, Result)
    Call UCheb(X, 0.005204792, TJ, TJ1, Result)
    Call UCheb(X, 0.004775217, TJ, TJ1, Result)
    Call UCheb(X, 0.003527781, TJ, TJ1, Result)
    Call UCheb(X, 0.002221948, TJ, TJ1, Result)
    Call UCheb(X, 0.002242968, TJ, TJ1, Result)
    Call UCheb(X, 0.002607959, TJ, TJ1, Result)
    Call UCheb(X, 0.001771285, TJ, TJ1, Result)
    Call UCheb(X, 0.0006694026, TJ, TJ1, Result)
    Call UCheb(X, -0.00148119, TJ, TJ1, Result)
    UTblN5N9 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 10)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N10(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.061862 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.42536, TJ, TJ1, Result)
    Call UCheb(X, -3.49671, TJ, TJ1, Result)
    Call UCheb(X, -0.8587658, TJ, TJ1, Result)
    Call UCheb(X, -0.1812005, TJ, TJ1, Result)
    Call UCheb(X, -0.05427637, TJ, TJ1, Result)
    Call UCheb(X, -0.01515702, TJ, TJ1, Result)
    Call UCheb(X, -0.0005406867, TJ, TJ1, Result)
    Call UCheb(X, 0.004796295, TJ, TJ1, Result)
    Call UCheb(X, 0.005237591, TJ, TJ1, Result)
    Call UCheb(X, 0.003654249, TJ, TJ1, Result)
    Call UCheb(X, 0.002181165, TJ, TJ1, Result)
    Call UCheb(X, 0.002011665, TJ, TJ1, Result)
    Call UCheb(X, 0.002417927, TJ, TJ1, Result)
    Call UCheb(X, 0.00253488, TJ, TJ1, Result)
    Call UCheb(X, 0.001791255, TJ, TJ1, Result)
    Call UCheb(X, 0.00001871512, TJ, TJ1, Result)
    UTblN5N10 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.115427 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.539959, TJ, TJ1, Result)
    Call UCheb(X, -3.652998, TJ, TJ1, Result)
    Call UCheb(X, -0.9196503, TJ, TJ1, Result)
    Call UCheb(X, -0.2054363, TJ, TJ1, Result)
    Call UCheb(X, -0.06618848, TJ, TJ1, Result)
    Call UCheb(X, -0.02109411, TJ, TJ1, Result)
    Call UCheb(X, -0.002786668, TJ, TJ1, Result)
    Call UCheb(X, 0.004215648, TJ, TJ1, Result)
    Call UCheb(X, 0.00548422, TJ, TJ1, Result)
    Call UCheb(X, 0.003935991, TJ, TJ1, Result)
    Call UCheb(X, 0.002396191, TJ, TJ1, Result)
    Call UCheb(X, 0.001894177, TJ, TJ1, Result)
    Call UCheb(X, 0.002206979, TJ, TJ1, Result)
    Call UCheb(X, 0.002519055, TJ, TJ1, Result)
    Call UCheb(X, 0.002210326, TJ, TJ1, Result)
    Call UCheb(X, 0.001189679, TJ, TJ1, Result)
    UTblN5N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.162278 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.644007, TJ, TJ1, Result)
    Call UCheb(X, -3.796173, TJ, TJ1, Result)
    Call UCheb(X, -0.9771177, TJ, TJ1, Result)
    Call UCheb(X, -0.2290043, TJ, TJ1, Result)
    Call UCheb(X, -0.07794686, TJ, TJ1, Result)
    Call UCheb(X, -0.0270211, TJ, TJ1, Result)
    Call UCheb(X, -0.005185959, TJ, TJ1, Result)
    Call UCheb(X, 0.003416259, TJ, TJ1, Result)
    Call UCheb(X, 0.005592056, TJ, TJ1, Result)
    Call UCheb(X, 0.00420153, TJ, TJ1, Result)
    Call UCheb(X, 0.002754365, TJ, TJ1, Result)
    Call UCheb(X, 0.001978945, TJ, TJ1, Result)
    Call UCheb(X, 0.002012032, TJ, TJ1, Result)
    Call UCheb(X, 0.002304579, TJ, TJ1, Result)
    Call UCheb(X, 0.002100378, TJ, TJ1, Result)
    Call UCheb(X, 0.001728269, TJ, TJ1, Result)
    UTblN5N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.203616 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.73912, TJ, TJ1, Result)
    Call UCheb(X, -3.928117, TJ, TJ1, Result)
    Call UCheb(X, -1.031605, TJ, TJ1, Result)
    Call UCheb(X, -0.2519403, TJ, TJ1, Result)
    Call UCheb(X, -0.08962648, TJ, TJ1, Result)
    Call UCheb(X, -0.03292183, TJ, TJ1, Result)
    Call UCheb(X, -0.007809293, TJ, TJ1, Result)
    Call UCheb(X, 0.002465156, TJ, TJ1, Result)
    Call UCheb(X, 0.005456278, TJ, TJ1, Result)
    Call UCheb(X, 0.004446055, TJ, TJ1, Result)
    Call UCheb(X, 0.00310949, TJ, TJ1, Result)
    Call UCheb(X, 0.002218256, TJ, TJ1, Result)
    Call UCheb(X, 0.001941479, TJ, TJ1, Result)
    Call UCheb(X, 0.002058603, TJ, TJ1, Result)
    Call UCheb(X, 0.001824402, TJ, TJ1, Result)
    Call UCheb(X, 0.001830947, TJ, TJ1, Result)
    UTblN5N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.24037 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.826559, TJ, TJ1, Result)
    Call UCheb(X, -4.05037, TJ, TJ1, Result)
    Call UCheb(X, -1.083408, TJ, TJ1, Result)
    Call UCheb(X, -0.2743164, TJ, TJ1, Result)
    Call UCheb(X, -0.101203, TJ, TJ1, Result)
    Call UCheb(X, -0.03884686, TJ, TJ1, Result)
    Call UCheb(X, -0.01059656, TJ, TJ1, Result)
    Call UCheb(X, 0.001327521, TJ, TJ1, Result)
    Call UCheb(X, 0.005134026, TJ, TJ1, Result)
    Call UCheb(X, 0.004584201, TJ, TJ1, Result)
    Call UCheb(X, 0.003440618, TJ, TJ1, Result)
    Call UCheb(X, 0.002524133, TJ, TJ1, Result)
    Call UCheb(X, 0.001990007, TJ, TJ1, Result)
    Call UCheb(X, 0.001887334, TJ, TJ1, Result)
    Call UCheb(X, 0.001534977, TJ, TJ1, Result)
    Call UCheb(X, 0.001705395, TJ, TJ1, Result)
    UTblN5N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.851572, TJ, TJ1, Result)
    Call UCheb(X, -4.082033, TJ, TJ1, Result)
    Call UCheb(X, -1.095983, TJ, TJ1, Result)
    Call UCheb(X, -0.2814595, TJ, TJ1, Result)
    Call UCheb(X, -0.1073148, TJ, TJ1, Result)
    Call UCheb(X, -0.04420213, TJ, TJ1, Result)
    Call UCheb(X, -0.01517175, TJ, TJ1, Result)
    Call UCheb(X, -0.00234418, TJ, TJ1, Result)
    Call UCheb(X, 0.002371393, TJ, TJ1, Result)
    Call UCheb(X, 0.002711443, TJ, TJ1, Result)
    Call UCheb(X, 0.002228569, TJ, TJ1, Result)
    Call UCheb(X, 0.001683483, TJ, TJ1, Result)
    Call UCheb(X, 0.001267112, TJ, TJ1, Result)
    Call UCheb(X, 0.001156044, TJ, TJ1, Result)
    Call UCheb(X, 0.0009131316, TJ, TJ1, Result)
    Call UCheb(X, 0.001301023, TJ, TJ1, Result)
    UTblN5N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 16)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N16(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.85221, TJ, TJ1, Result)
    Call UCheb(X, -4.077482, TJ, TJ1, Result)
    Call UCheb(X, -1.091186, TJ, TJ1, Result)
    Call UCheb(X, -0.2797282, TJ, TJ1, Result)
    Call UCheb(X, -0.1084994, TJ, TJ1, Result)
    Call UCheb(X, -0.04667054, TJ, TJ1, Result)
    Call UCheb(X, -0.01843909, TJ, TJ1, Result)
    Call UCheb(X, -0.005456732, TJ, TJ1, Result)
    Call UCheb(X, -0.000503983, TJ, TJ1, Result)
    Call UCheb(X, 0.0004723508, TJ, TJ1, Result)
    Call UCheb(X, 0.0003940608, TJ, TJ1, Result)
    Call UCheb(X, 0.0001478285, TJ, TJ1, Result)
    Call UCheb(X, -0.0001649144, TJ, TJ1, Result)
    Call UCheb(X, -0.0004237703, TJ, TJ1, Result)
    Call UCheb(X, -0.000470741, TJ, TJ1, Result)
    Call UCheb(X, -0.0001874293, TJ, TJ1, Result)
    UTblN5N16 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 17)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N17(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.851752, TJ, TJ1, Result)
    Call UCheb(X, -4.071259, TJ, TJ1, Result)
    Call UCheb(X, -1.0847, TJ, TJ1, Result)
    Call UCheb(X, -0.2758898, TJ, TJ1, Result)
    Call UCheb(X, -0.1073846, TJ, TJ1, Result)
    Call UCheb(X, -0.04684838, TJ, TJ1, Result)
    Call UCheb(X, -0.01964936, TJ, TJ1, Result)
    Call UCheb(X, -0.006782442, TJ, TJ1, Result)
    Call UCheb(X, -0.001956362, TJ, TJ1, Result)
    Call UCheb(X, -0.0005984727, TJ, TJ1, Result)
    Call UCheb(X, -0.0005196936, TJ, TJ1, Result)
    Call UCheb(X, -0.0005558262, TJ, TJ1, Result)
    Call UCheb(X, -0.0008690746, TJ, TJ1, Result)
    Call UCheb(X, -0.001364855, TJ, TJ1, Result)
    Call UCheb(X, -0.001401006, TJ, TJ1, Result)
    Call UCheb(X, -0.001546748, TJ, TJ1, Result)
    UTblN5N17 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 18)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N18(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.85084, TJ, TJ1, Result)
    Call UCheb(X, -4.064799, TJ, TJ1, Result)
    Call UCheb(X, -1.077651, TJ, TJ1, Result)
    Call UCheb(X, -0.2712659, TJ, TJ1, Result)
    Call UCheb(X, -0.1049217, TJ, TJ1, Result)
    Call UCheb(X, -0.04571333, TJ, TJ1, Result)
    Call UCheb(X, -0.01929809, TJ, TJ1, Result)
    Call UCheb(X, -0.006752044, TJ, TJ1, Result)
    Call UCheb(X, -0.001949464, TJ, TJ1, Result)
    Call UCheb(X, -0.0003896101, TJ, TJ1, Result)
    Call UCheb(X, -0.0000461446, TJ, TJ1, Result)
    Call UCheb(X, 0.0001384357, TJ, TJ1, Result)
    Call UCheb(X, -0.00006489113, TJ, TJ1, Result)
    Call UCheb(X, -0.0006445725, TJ, TJ1, Result)
    Call UCheb(X, -0.0008945636, TJ, TJ1, Result)
    Call UCheb(X, -0.001424653, TJ, TJ1, Result)
    UTblN5N18 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 19)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N19(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.850027, TJ, TJ1, Result)
    Call UCheb(X, -4.059159, TJ, TJ1, Result)
    Call UCheb(X, -1.071106, TJ, TJ1, Result)
    Call UCheb(X, -0.266996, TJ, TJ1, Result)
    Call UCheb(X, -0.102278, TJ, TJ1, Result)
    Call UCheb(X, -0.04442555, TJ, TJ1, Result)
    Call UCheb(X, -0.01851335, TJ, TJ1, Result)
    Call UCheb(X, -0.006433865, TJ, TJ1, Result)
    Call UCheb(X, -0.001514465, TJ, TJ1, Result)
    Call UCheb(X, 0.0001332989, TJ, TJ1, Result)
    Call UCheb(X, 0.0008606099, TJ, TJ1, Result)
    Call UCheb(X, 0.001341945, TJ, TJ1, Result)
    Call UCheb(X, 0.001402164, TJ, TJ1, Result)
    Call UCheb(X, 0.001039761, TJ, TJ1, Result)
    Call UCheb(X, 0.0005512831, TJ, TJ1, Result)
    Call UCheb(X, -0.00003284427, TJ, TJ1, Result)
    UTblN5N19 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 20)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N20(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.849651, TJ, TJ1, Result)
    Call UCheb(X, -4.054729, TJ, TJ1, Result)
    Call UCheb(X, -1.065747, TJ, TJ1, Result)
    Call UCheb(X, -0.2636243, TJ, TJ1, Result)
    Call UCheb(X, -0.1003234, TJ, TJ1, Result)
    Call UCheb(X, -0.04372789, TJ, TJ1, Result)
    Call UCheb(X, -0.01831551, TJ, TJ1, Result)
    Call UCheb(X, -0.00676309, TJ, TJ1, Result)
    Call UCheb(X, -0.001830626, TJ, TJ1, Result)
    Call UCheb(X, -0.0002122384, TJ, TJ1, Result)
    Call UCheb(X, 0.0008108328, TJ, TJ1, Result)
    Call UCheb(X, 0.001557983, TJ, TJ1, Result)
    Call UCheb(X, 0.001945666, TJ, TJ1, Result)
    Call UCheb(X, 0.001965696, TJ, TJ1, Result)
    Call UCheb(X, 0.001493236, TJ, TJ1, Result)
    Call UCheb(X, 0.001162591, TJ, TJ1, Result)
    UTblN5N20 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 21)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N21(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.849649, TJ, TJ1, Result)
    Call UCheb(X, -4.051155, TJ, TJ1, Result)
    Call UCheb(X, -1.06143, TJ, TJ1, Result)
    Call UCheb(X, -0.2608869, TJ, TJ1, Result)
    Call UCheb(X, -0.09902788, TJ, TJ1, Result)
    Call UCheb(X, -0.04346562, TJ, TJ1, Result)
    Call UCheb(X, -0.01874709, TJ, TJ1, Result)
    Call UCheb(X, -0.007682887, TJ, TJ1, Result)
    Call UCheb(X, -0.003026206, TJ, TJ1, Result)
    Call UCheb(X, -0.001534551, TJ, TJ1, Result)
    Call UCheb(X, -0.0004990575, TJ, TJ1, Result)
    Call UCheb(X, 0.0003713334, TJ, TJ1, Result)
    Call UCheb(X, 0.0009737011, TJ, TJ1, Result)
    Call UCheb(X, 0.001304571, TJ, TJ1, Result)
    Call UCheb(X, 0.00113311, TJ, TJ1, Result)
    Call UCheb(X, 0.001123457, TJ, TJ1, Result)
    UTblN5N21 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 22)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N22(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.849598, TJ, TJ1, Result)
    Call UCheb(X, -4.047605, TJ, TJ1, Result)
    Call UCheb(X, -1.057264, TJ, TJ1, Result)
    Call UCheb(X, -0.2579513, TJ, TJ1, Result)
    Call UCheb(X, -0.09749602, TJ, TJ1, Result)
    Call UCheb(X, -0.04275137, TJ, TJ1, Result)
    Call UCheb(X, -0.01881768, TJ, TJ1, Result)
    Call UCheb(X, -0.008177374, TJ, TJ1, Result)
    Call UCheb(X, -0.003981056, TJ, TJ1, Result)
    Call UCheb(X, -0.00269629, TJ, TJ1, Result)
    Call UCheb(X, -0.001886803, TJ, TJ1, Result)
    Call UCheb(X, -0.001085378, TJ, TJ1, Result)
    Call UCheb(X, -0.0004675242, TJ, TJ1, Result)
    Call UCheb(X, -0.00005426367, TJ, TJ1, Result)
    Call UCheb(X, 0.0001039613, TJ, TJ1, Result)
    Call UCheb(X, 0.0002662378, TJ, TJ1, Result)
    UTblN5N22 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 23)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N23(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.849269, TJ, TJ1, Result)
    Call UCheb(X, -4.043761, TJ, TJ1, Result)
    Call UCheb(X, -1.052735, TJ, TJ1, Result)
    Call UCheb(X, -0.2544683, TJ, TJ1, Result)
    Call UCheb(X, -0.09517503, TJ, TJ1, Result)
    Call UCheb(X, -0.04112082, TJ, TJ1, Result)
    Call UCheb(X, -0.0178207, TJ, TJ1, Result)
    Call UCheb(X, -0.007549483, TJ, TJ1, Result)
    Call UCheb(X, -0.003747329, TJ, TJ1, Result)
    Call UCheb(X, -0.002694263, TJ, TJ1, Result)
    Call UCheb(X, -0.002147141, TJ, TJ1, Result)
    Call UCheb(X, -0.001526209, TJ, TJ1, Result)
    Call UCheb(X, -0.001039173, TJ, TJ1, Result)
    Call UCheb(X, -0.0007235615, TJ, TJ1, Result)
    Call UCheb(X, -0.0004656546, TJ, TJ1, Result)
    Call UCheb(X, -0.0003014423, TJ, TJ1, Result)
    UTblN5N23 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 24)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N24(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.848925, TJ, TJ1, Result)
    Call UCheb(X, -4.040178, TJ, TJ1, Result)
    Call UCheb(X, -1.048355, TJ, TJ1, Result)
    Call UCheb(X, -0.2510198, TJ, TJ1, Result)
    Call UCheb(X, -0.09261134, TJ, TJ1, Result)
    Call UCheb(X, -0.03915864, TJ, TJ1, Result)
    Call UCheb(X, -0.01627423, TJ, TJ1, Result)
    Call UCheb(X, -0.006307345, TJ, TJ1, Result)
    Call UCheb(X, -0.002732992, TJ, TJ1, Result)
    Call UCheb(X, -0.001869652, TJ, TJ1, Result)
    Call UCheb(X, -0.001494176, TJ, TJ1, Result)
    Call UCheb(X, -0.001047533, TJ, TJ1, Result)
    Call UCheb(X, -0.0007178439, TJ, TJ1, Result)
    Call UCheb(X, -0.0005424171, TJ, TJ1, Result)
    Call UCheb(X, -0.0003829195, TJ, TJ1, Result)
    Call UCheb(X, -0.000284081, TJ, TJ1, Result)
    UTblN5N24 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 25)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N25(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.848937, TJ, TJ1, Result)
    Call UCheb(X, -4.037512, TJ, TJ1, Result)
    Call UCheb(X, -1.044866, TJ, TJ1, Result)
    Call UCheb(X, -0.2483269, TJ, TJ1, Result)
    Call UCheb(X, -0.09063682, TJ, TJ1, Result)
    Call UCheb(X, -0.03767778, TJ, TJ1, Result)
    Call UCheb(X, -0.0150854, TJ, TJ1, Result)
    Call UCheb(X, -0.005332756, TJ, TJ1, Result)
    Call UCheb(X, -0.001881511, TJ, TJ1, Result)
    Call UCheb(X, -0.001124041, TJ, TJ1, Result)
    Call UCheb(X, -0.0008368456, TJ, TJ1, Result)
    Call UCheb(X, -0.0004930499, TJ, TJ1, Result)
    Call UCheb(X, -0.000277963, TJ, TJ1, Result)
    Call UCheb(X, -0.0002029528, TJ, TJ1, Result)
    Call UCheb(X, -0.0001658678, TJ, TJ1, Result)
    Call UCheb(X, -0.0001289695, TJ, TJ1, Result)
    UTblN5N25 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 26)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N26(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.849416, TJ, TJ1, Result)
    Call UCheb(X, -4.035915, TJ, TJ1, Result)
    Call UCheb(X, -1.042493, TJ, TJ1, Result)
    Call UCheb(X, -0.2466021, TJ, TJ1, Result)
    Call UCheb(X, -0.08956432, TJ, TJ1, Result)
    Call UCheb(X, -0.03698914, TJ, TJ1, Result)
    Call UCheb(X, -0.01465689, TJ, TJ1, Result)
    Call UCheb(X, -0.005035254, TJ, TJ1, Result)
    Call UCheb(X, -0.001674614, TJ, TJ1, Result)
    Call UCheb(X, -0.0009492734, TJ, TJ1, Result)
    Call UCheb(X, -0.0007014021, TJ, TJ1, Result)
    Call UCheb(X, -0.0003944953, TJ, TJ1, Result)
    Call UCheb(X, -0.000225575, TJ, TJ1, Result)
    Call UCheb(X, -0.0002075841, TJ, TJ1, Result)
    Call UCheb(X, -0.000198933, TJ, TJ1, Result)
    Call UCheb(X, -0.0002134862, TJ, TJ1, Result)
    UTblN5N26 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 27)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N27(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.85007, TJ, TJ1, Result)
    Call UCheb(X, -4.034815, TJ, TJ1, Result)
    Call UCheb(X, -1.04065, TJ, TJ1, Result)
    Call UCheb(X, -0.2453117, TJ, TJ1, Result)
    Call UCheb(X, -0.08886426, TJ, TJ1, Result)
    Call UCheb(X, -0.03661702, TJ, TJ1, Result)
    Call UCheb(X, -0.01452346, TJ, TJ1, Result)
    Call UCheb(X, -0.005002476, TJ, TJ1, Result)
    Call UCheb(X, -0.001720126, TJ, TJ1, Result)
    Call UCheb(X, -0.0010014, TJ, TJ1, Result)
    Call UCheb(X, -0.0007729826, TJ, TJ1, Result)
    Call UCheb(X, -0.000474064, TJ, TJ1, Result)
    Call UCheb(X, -0.0003206333, TJ, TJ1, Result)
    Call UCheb(X, -0.0003366093, TJ, TJ1, Result)
    Call UCheb(X, -0.0003193471, TJ, TJ1, Result)
    Call UCheb(X, -0.0003804091, TJ, TJ1, Result)
    UTblN5N27 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 28)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N28(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.850668, TJ, TJ1, Result)
    Call UCheb(X, -4.033786, TJ, TJ1, Result)
    Call UCheb(X, -1.038853, TJ, TJ1, Result)
    Call UCheb(X, -0.2440281, TJ, TJ1, Result)
    Call UCheb(X, -0.0880602, TJ, TJ1, Result)
    Call UCheb(X, -0.03612883, TJ, TJ1, Result)
    Call UCheb(X, -0.01420436, TJ, TJ1, Result)
    Call UCheb(X, -0.004787982, TJ, TJ1, Result)
    Call UCheb(X, -0.00153523, TJ, TJ1, Result)
    Call UCheb(X, -0.0008263121, TJ, TJ1, Result)
    Call UCheb(X, -0.0005849609, TJ, TJ1, Result)
    Call UCheb(X, -0.0002863967, TJ, TJ1, Result)
    Call UCheb(X, -0.000139161, TJ, TJ1, Result)
    Call UCheb(X, -0.0001720294, TJ, TJ1, Result)
    Call UCheb(X, -0.0001952273, TJ, TJ1, Result)
    Call UCheb(X, -0.0002901413, TJ, TJ1, Result)
    UTblN5N28 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 29)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N29(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.851217, TJ, TJ1, Result)
    Call UCheb(X, -4.032834, TJ, TJ1, Result)
    Call UCheb(X, -1.037113, TJ, TJ1, Result)
    Call UCheb(X, -0.2427762, TJ, TJ1, Result)
    Call UCheb(X, -0.08719146, TJ, TJ1, Result)
    Call UCheb(X, -0.03557172, TJ, TJ1, Result)
    Call UCheb(X, -0.01375498, TJ, TJ1, Result)
    Call UCheb(X, -0.004452033, TJ, TJ1, Result)
    Call UCheb(X, -0.001187516, TJ, TJ1, Result)
    Call UCheb(X, -0.0004916936, TJ, TJ1, Result)
    Call UCheb(X, -0.0002065533, TJ, TJ1, Result)
    Call UCheb(X, 0.0001067301, TJ, TJ1, Result)
    Call UCheb(X, 0.0002615824, TJ, TJ1, Result)
    Call UCheb(X, 0.0002432244, TJ, TJ1, Result)
    Call UCheb(X, 0.0001417795, TJ, TJ1, Result)
    Call UCheb(X, 0.00004710038, TJ, TJ1, Result)
    UTblN5N29 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.851845, TJ, TJ1, Result)
    Call UCheb(X, -4.032148, TJ, TJ1, Result)
    Call UCheb(X, -1.035679, TJ, TJ1, Result)
    Call UCheb(X, -0.2417758, TJ, TJ1, Result)
    Call UCheb(X, -0.0865533, TJ, TJ1, Result)
    Call UCheb(X, -0.03522132, TJ, TJ1, Result)
    Call UCheb(X, -0.01352106, TJ, TJ1, Result)
    Call UCheb(X, -0.004326911, TJ, TJ1, Result)
    Call UCheb(X, -0.001064969, TJ, TJ1, Result)
    Call UCheb(X, -0.0003813321, TJ, TJ1, Result)
    Call UCheb(X, -0.00005683881, TJ, TJ1, Result)
    Call UCheb(X, 0.0002813346, TJ, TJ1, Result)
    Call UCheb(X, 0.0004627085, TJ, TJ1, Result)
    Call UCheb(X, 0.0004832107, TJ, TJ1, Result)
    Call UCheb(X, 0.0003519336, TJ, TJ1, Result)
    Call UCheb(X, 0.000288853, TJ, TJ1, Result)
    UTblN5N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 5, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN5N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.25 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.87794, TJ, TJ1, Result)
    Call UCheb(X, -4.039324, TJ, TJ1, Result)
    Call UCheb(X, -1.022243, TJ, TJ1, Result)
    Call UCheb(X, -0.2305825, TJ, TJ1, Result)
    Call UCheb(X, -0.07960119, TJ, TJ1, Result)
    Call UCheb(X, -0.03112, TJ, TJ1, Result)
    Call UCheb(X, -0.01138868, TJ, TJ1, Result)
    Call UCheb(X, -0.003418164, TJ, TJ1, Result)
    Call UCheb(X, -0.000917452, TJ, TJ1, Result)
    Call UCheb(X, -0.0005489617, TJ, TJ1, Result)
    Call UCheb(X, -0.0003878301, TJ, TJ1, Result)
    Call UCheb(X, -0.0001302233, TJ, TJ1, Result)
    Call UCheb(X, 0.00001054113, TJ, TJ1, Result)
    Call UCheb(X, 0.00002458862, TJ, TJ1, Result)
    Call UCheb(X, -0.000004186591, TJ, TJ1, Result)
    Call UCheb(X, -0.00002623412, TJ, TJ1, Result)
    UTblN5N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 6)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N6(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 2.882307 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.054075, TJ, TJ1, Result)
    Call UCheb(X, -2.998804, TJ, TJ1, Result)
    Call UCheb(X, -0.6681518, TJ, TJ1, Result)
    Call UCheb(X, -0.1067578, TJ, TJ1, Result)
    Call UCheb(X, -0.01709435, TJ, TJ1, Result)
    Call UCheb(X, 0.0009952661, TJ, TJ1, Result)
    Call UCheb(X, 0.0036417, TJ, TJ1, Result)
    Call UCheb(X, 0.002304572, TJ, TJ1, Result)
    Call UCheb(X, 0.003336275, TJ, TJ1, Result)
    Call UCheb(X, 0.004770385, TJ, TJ1, Result)
    Call UCheb(X, 0.005401891, TJ, TJ1, Result)
    Call UCheb(X, 0.002246148, TJ, TJ1, Result)
    Call UCheb(X, -0.001442663, TJ, TJ1, Result)
    Call UCheb(X, -0.002502866, TJ, TJ1, Result)
    Call UCheb(X, -0.002105855, TJ, TJ1, Result)
    Call UCheb(X, -0.0004739371, TJ, TJ1, Result)
    UTblN6N6 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 7)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N7(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3# - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.265287, TJ, TJ1, Result)
    Call UCheb(X, -3.274613, TJ, TJ1, Result)
    Call UCheb(X, -0.7582352, TJ, TJ1, Result)
    Call UCheb(X, -0.1334293, TJ, TJ1, Result)
    Call UCheb(X, -0.02915502, TJ, TJ1, Result)
    Call UCheb(X, -0.004108091, TJ, TJ1, Result)
    Call UCheb(X, 0.001546701, TJ, TJ1, Result)
    Call UCheb(X, 0.002298827, TJ, TJ1, Result)
    Call UCheb(X, 0.002891501, TJ, TJ1, Result)
    Call UCheb(X, 0.004313717, TJ, TJ1, Result)
    Call UCheb(X, 0.004989501, TJ, TJ1, Result)
    Call UCheb(X, 0.003914594, TJ, TJ1, Result)
    Call UCheb(X, 0.001062372, TJ, TJ1, Result)
    Call UCheb(X, -0.001158841, TJ, TJ1, Result)
    Call UCheb(X, -0.001596443, TJ, TJ1, Result)
    Call UCheb(X, -0.001185662, TJ, TJ1, Result)
    UTblN6N7 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 8)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N8(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.098387 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.450954, TJ, TJ1, Result)
    Call UCheb(X, -3.520462, TJ, TJ1, Result)
    Call UCheb(X, -0.8420299, TJ, TJ1, Result)
    Call UCheb(X, -0.1604853, TJ, TJ1, Result)
    Call UCheb(X, -0.0416584, TJ, TJ1, Result)
    Call UCheb(X, -0.01008756, TJ, TJ1, Result)
    Call UCheb(X, -0.0006723402, TJ, TJ1, Result)
    Call UCheb(X, 0.001843521, TJ, TJ1, Result)
    Call UCheb(X, 0.002883405, TJ, TJ1, Result)
    Call UCheb(X, 0.00372098, TJ, TJ1, Result)
    Call UCheb(X, 0.004301709, TJ, TJ1, Result)
    Call UCheb(X, 0.003948034, TJ, TJ1, Result)
    Call UCheb(X, 0.002776243, TJ, TJ1, Result)
    Call UCheb(X, 0.0008623736, TJ, TJ1, Result)
    Call UCheb(X, -0.0003742068, TJ, TJ1, Result)
    Call UCheb(X, -0.0009796927, TJ, TJ1, Result)
    UTblN6N8 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 9)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N9(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.181981 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.616113, TJ, TJ1, Result)
    Call UCheb(X, -3.74165, TJ, TJ1, Result)
    Call UCheb(X, -0.9204487, TJ, TJ1, Result)
    Call UCheb(X, -0.1873068, TJ, TJ1, Result)
    Call UCheb(X, -0.05446794, TJ, TJ1, Result)
    Call UCheb(X, -0.01632286, TJ, TJ1, Result)
    Call UCheb(X, -0.003266481, TJ, TJ1, Result)
    Call UCheb(X, 0.001280067, TJ, TJ1, Result)
    Call UCheb(X, 0.002780687, TJ, TJ1, Result)
    Call UCheb(X, 0.003480242, TJ, TJ1, Result)
    Call UCheb(X, 0.0035922, TJ, TJ1, Result)
    Call UCheb(X, 0.003581019, TJ, TJ1, Result)
    Call UCheb(X, 0.003264231, TJ, TJ1, Result)
    Call UCheb(X, 0.002347174, TJ, TJ1, Result)
    Call UCheb(X, 0.001167535, TJ, TJ1, Result)
    Call UCheb(X, -0.0001092185, TJ, TJ1, Result)
    UTblN6N9 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 10)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N10(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.253957 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.764382, TJ, TJ1, Result)
    Call UCheb(X, -3.942366, TJ, TJ1, Result)
    Call UCheb(X, -0.9939896, TJ, TJ1, Result)
    Call UCheb(X, -0.2137812, TJ, TJ1, Result)
    Call UCheb(X, -0.0672027, TJ, TJ1, Result)
    Call UCheb(X, -0.0228107, TJ, TJ1, Result)
    Call UCheb(X, -0.00590106, TJ, TJ1, Result)
    Call UCheb(X, 0.0003824937, TJ, TJ1, Result)
    Call UCheb(X, 0.002802812, TJ, TJ1, Result)
    Call UCheb(X, 0.003258132, TJ, TJ1, Result)
    Call UCheb(X, 0.003233536, TJ, TJ1, Result)
    Call UCheb(X, 0.00308553, TJ, TJ1, Result)
    Call UCheb(X, 0.003212151, TJ, TJ1, Result)
    Call UCheb(X, 0.003001329, TJ, TJ1, Result)
    Call UCheb(X, 0.002226048, TJ, TJ1, Result)
    Call UCheb(X, 0.001035298, TJ, TJ1, Result)
    UTblN6N10 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.316625 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.898597, TJ, TJ1, Result)
    Call UCheb(X, -4.12571, TJ, TJ1, Result)
    Call UCheb(X, -1.063297, TJ, TJ1, Result)
    Call UCheb(X, -0.2396852, TJ, TJ1, Result)
    Call UCheb(X, -0.07990126, TJ, TJ1, Result)
    Call UCheb(X, -0.02927977, TJ, TJ1, Result)
    Call UCheb(X, -0.0087265, TJ, TJ1, Result)
    Call UCheb(X, -0.0005858745, TJ, TJ1, Result)
    Call UCheb(X, 0.00265459, TJ, TJ1, Result)
    Call UCheb(X, 0.003217736, TJ, TJ1, Result)
    Call UCheb(X, 0.00298977, TJ, TJ1, Result)
    Call UCheb(X, 0.002768493, TJ, TJ1, Result)
    Call UCheb(X, 0.002924364, TJ, TJ1, Result)
    Call UCheb(X, 0.003140215, TJ, TJ1, Result)
    Call UCheb(X, 0.002647914, TJ, TJ1, Result)
    Call UCheb(X, 0.001924802, TJ, TJ1, Result)
    UTblN6N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.371709 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.020941, TJ, TJ1, Result)
    Call UCheb(X, -4.29425, TJ, TJ1, Result)
    Call UCheb(X, -1.128842, TJ, TJ1, Result)
    Call UCheb(X, -0.2650389, TJ, TJ1, Result)
    Call UCheb(X, -0.09248611, TJ, TJ1, Result)
    Call UCheb(X, -0.0357851, TJ, TJ1, Result)
    Call UCheb(X, -0.01162852, TJ, TJ1, Result)
    Call UCheb(X, -0.001746982, TJ, TJ1, Result)
    Call UCheb(X, 0.002454209, TJ, TJ1, Result)
    Call UCheb(X, 0.003128042, TJ, TJ1, Result)
    Call UCheb(X, 0.00293665, TJ, TJ1, Result)
    Call UCheb(X, 0.002530794, TJ, TJ1, Result)
    Call UCheb(X, 0.002665192, TJ, TJ1, Result)
    Call UCheb(X, 0.002994144, TJ, TJ1, Result)
    Call UCheb(X, 0.002662249, TJ, TJ1, Result)
    Call UCheb(X, 0.002368541, TJ, TJ1, Result)
    UTblN6N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.420526 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.133167, TJ, TJ1, Result)
    Call UCheb(X, -4.450016, TJ, TJ1, Result)
    Call UCheb(X, -1.191088, TJ, TJ1, Result)
    Call UCheb(X, -0.289822, TJ, TJ1, Result)
    Call UCheb(X, -0.1050249, TJ, TJ1, Result)
    Call UCheb(X, -0.04226901, TJ, TJ1, Result)
    Call UCheb(X, -0.01471113, TJ, TJ1, Result)
    Call UCheb(X, -0.00300747, TJ, TJ1, Result)
    Call UCheb(X, 0.00204942, TJ, TJ1, Result)
    Call UCheb(X, 0.003059074, TJ, TJ1, Result)
    Call UCheb(X, 0.002881249, TJ, TJ1, Result)
    Call UCheb(X, 0.00245278, TJ, TJ1, Result)
    Call UCheb(X, 0.002441805, TJ, TJ1, Result)
    Call UCheb(X, 0.002787493, TJ, TJ1, Result)
    Call UCheb(X, 0.002483957, TJ, TJ1, Result)
    Call UCheb(X, 0.00248159, TJ, TJ1, Result)
    UTblN6N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.45 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.201268, TJ, TJ1, Result)
    Call UCheb(X, -4.542568, TJ, TJ1, Result)
    Call UCheb(X, -1.226965, TJ, TJ1, Result)
    Call UCheb(X, -0.3046029, TJ, TJ1, Result)
    Call UCheb(X, -0.1136657, TJ, TJ1, Result)
    Call UCheb(X, -0.04786757, TJ, TJ1, Result)
    Call UCheb(X, -0.01843748, TJ, TJ1, Result)
    Call UCheb(X, -0.005588022, TJ, TJ1, Result)
    Call UCheb(X, 0.0002253029, TJ, TJ1, Result)
    Call UCheb(X, 0.001667188, TJ, TJ1, Result)
    Call UCheb(X, 0.00178833, TJ, TJ1, Result)
    Call UCheb(X, 0.001474545, TJ, TJ1, Result)
    Call UCheb(X, 0.001540494, TJ, TJ1, Result)
    Call UCheb(X, 0.001951188, TJ, TJ1, Result)
    Call UCheb(X, 0.001863323, TJ, TJ1, Result)
    Call UCheb(X, 0.002220904, TJ, TJ1, Result)
    UTblN6N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.45 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.195689, TJ, TJ1, Result)
    Call UCheb(X, -4.526567, TJ, TJ1, Result)
    Call UCheb(X, -1.213617, TJ, TJ1, Result)
    Call UCheb(X, -0.2975035, TJ, TJ1, Result)
    Call UCheb(X, -0.111848, TJ, TJ1, Result)
    Call UCheb(X, -0.04859142, TJ, TJ1, Result)
    Call UCheb(X, -0.02083312, TJ, TJ1, Result)
    Call UCheb(X, -0.00829872, TJ, TJ1, Result)
    Call UCheb(X, -0.002766708, TJ, TJ1, Result)
    Call UCheb(X, -0.001026356, TJ, TJ1, Result)
    Call UCheb(X, -0.0009093113, TJ, TJ1, Result)
    Call UCheb(X, -0.001135168, TJ, TJ1, Result)
    Call UCheb(X, -0.001136376, TJ, TJ1, Result)
    Call UCheb(X, -0.000819087, TJ, TJ1, Result)
    Call UCheb(X, -0.0004435972, TJ, TJ1, Result)
    Call UCheb(X, 0.0001413129, TJ, TJ1, Result)
    UTblN6N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.45 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.166269, TJ, TJ1, Result)
    Call UCheb(X, -4.427399, TJ, TJ1, Result)
    Call UCheb(X, -1.118239, TJ, TJ1, Result)
    Call UCheb(X, -0.2360847, TJ, TJ1, Result)
    Call UCheb(X, -0.07745885, TJ, TJ1, Result)
    Call UCheb(X, -0.03025041, TJ, TJ1, Result)
    Call UCheb(X, -0.01187179, TJ, TJ1, Result)
    Call UCheb(X, -0.004432089, TJ, TJ1, Result)
    Call UCheb(X, -0.001408451, TJ, TJ1, Result)
    Call UCheb(X, -0.0004388774, TJ, TJ1, Result)
    Call UCheb(X, -0.000279556, TJ, TJ1, Result)
    Call UCheb(X, -0.0002304136, TJ, TJ1, Result)
    Call UCheb(X, -0.0001258516, TJ, TJ1, Result)
    Call UCheb(X, -0.00004180236, TJ, TJ1, Result)
    Call UCheb(X, -0.000004388679, TJ, TJ1, Result)
    Call UCheb(X, 0.000004836027, TJ, TJ1, Result)
    UTblN6N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 6, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN6N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.45 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.18135, TJ, TJ1, Result)
    Call UCheb(X, -4.417919, TJ, TJ1, Result)
    Call UCheb(X, -1.094201, TJ, TJ1, Result)
    Call UCheb(X, -0.2195883, TJ, TJ1, Result)
    Call UCheb(X, -0.06818937, TJ, TJ1, Result)
    Call UCheb(X, -0.02514202, TJ, TJ1, Result)
    Call UCheb(X, -0.009125047, TJ, TJ1, Result)
    Call UCheb(X, -0.003022148, TJ, TJ1, Result)
    Call UCheb(X, -0.0007284181, TJ, TJ1, Result)
    Call UCheb(X, -0.0001157766, TJ, TJ1, Result)
    Call UCheb(X, -0.0001023752, TJ, TJ1, Result)
    Call UCheb(X, -0.0001127985, TJ, TJ1, Result)
    Call UCheb(X, -0.0000522169, TJ, TJ1, Result)
    Call UCheb(X, -0.000003516179, TJ, TJ1, Result)
    Call UCheb(X, 0.000009501398, TJ, TJ1, Result)
    Call UCheb(X, 0.00000938022, TJ, TJ1, Result)
    UTblN6N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 7)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N7(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.130495 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.501264, TJ, TJ1, Result)
    Call UCheb(X, -3.58479, TJ, TJ1, Result)
    Call UCheb(X, -0.8577311, TJ, TJ1, Result)
    Call UCheb(X, -0.1617002, TJ, TJ1, Result)
    Call UCheb(X, -0.04145186, TJ, TJ1, Result)
    Call UCheb(X, -0.01023462, TJ, TJ1, Result)
    Call UCheb(X, -0.001408251, TJ, TJ1, Result)
    Call UCheb(X, 0.0008626515, TJ, TJ1, Result)
    Call UCheb(X, 0.002072492, TJ, TJ1, Result)
    Call UCheb(X, 0.003722926, TJ, TJ1, Result)
    Call UCheb(X, 0.005095445, TJ, TJ1, Result)
    Call UCheb(X, 0.004842602, TJ, TJ1, Result)
    Call UCheb(X, 0.002751427, TJ, TJ1, Result)
    Call UCheb(X, 0.0002008927, TJ, TJ1, Result)
    Call UCheb(X, -0.0009892431, TJ, TJ1, Result)
    Call UCheb(X, -0.0008772386, TJ, TJ1, Result)
    UTblN7N7 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 8)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N8(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.24037 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.709965, TJ, TJ1, Result)
    Call UCheb(X, -3.862154, TJ, TJ1, Result)
    Call UCheb(X, -0.9504541, TJ, TJ1, Result)
    Call UCheb(X, -0.1900195, TJ, TJ1, Result)
    Call UCheb(X, -0.05439995, TJ, TJ1, Result)
    Call UCheb(X, -0.01678028, TJ, TJ1, Result)
    Call UCheb(X, -0.00448554, TJ, TJ1, Result)
    Call UCheb(X, -0.0004437047, TJ, TJ1, Result)
    Call UCheb(X, 0.001440092, TJ, TJ1, Result)
    Call UCheb(X, 0.003114227, TJ, TJ1, Result)
    Call UCheb(X, 0.004516569, TJ, TJ1, Result)
    Call UCheb(X, 0.004829457, TJ, TJ1, Result)
    Call UCheb(X, 0.00378755, TJ, TJ1, Result)
    Call UCheb(X, 0.001761866, TJ, TJ1, Result)
    Call UCheb(X, 0.0001991911, TJ, TJ1, Result)
    Call UCheb(X, -0.0004533481, TJ, TJ1, Result)
    UTblN7N8 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 9)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N9(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.334314 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.89655, TJ, TJ1, Result)
    Call UCheb(X, -4.112671, TJ, TJ1, Result)
    Call UCheb(X, -1.037277, TJ, TJ1, Result)
    Call UCheb(X, -0.2181695, TJ, TJ1, Result)
    Call UCheb(X, -0.0676519, TJ, TJ1, Result)
    Call UCheb(X, -0.02360116, TJ, TJ1, Result)
    Call UCheb(X, -0.00769596, TJ, TJ1, Result)
    Call UCheb(X, -0.001780578, TJ, TJ1, Result)
    Call UCheb(X, 0.0008963843, TJ, TJ1, Result)
    Call UCheb(X, 0.002616148, TJ, TJ1, Result)
    Call UCheb(X, 0.003852104, TJ, TJ1, Result)
    Call UCheb(X, 0.004390744, TJ, TJ1, Result)
    Call UCheb(X, 0.004014041, TJ, TJ1, Result)
    Call UCheb(X, 0.002888101, TJ, TJ1, Result)
    Call UCheb(X, 0.001467474, TJ, TJ1, Result)
    Call UCheb(X, 0.0004004611, TJ, TJ1, Result)
    UTblN7N9 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 10)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N10(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.41565 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.064844, TJ, TJ1, Result)
    Call UCheb(X, -4.340749, TJ, TJ1, Result)
    Call UCheb(X, -1.118888, TJ, TJ1, Result)
    Call UCheb(X, -0.245973, TJ, TJ1, Result)
    Call UCheb(X, -0.08097781, TJ, TJ1, Result)
    Call UCheb(X, -0.03057688, TJ, TJ1, Result)
    Call UCheb(X, -0.01097406, TJ, TJ1, Result)
    Call UCheb(X, -0.003209262, TJ, TJ1, Result)
    Call UCheb(X, 0.0004065641, TJ, TJ1, Result)
    Call UCheb(X, 0.002196677, TJ, TJ1, Result)
    Call UCheb(X, 0.003313994, TJ, TJ1, Result)
    Call UCheb(X, 0.003827157, TJ, TJ1, Result)
    Call UCheb(X, 0.003822284, TJ, TJ1, Result)
    Call UCheb(X, 0.00338909, TJ, TJ1, Result)
    Call UCheb(X, 0.00234085, TJ, TJ1, Result)
    Call UCheb(X, 0.001395172, TJ, TJ1, Result)
    UTblN7N10 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.486817 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.217795, TJ, TJ1, Result)
    Call UCheb(X, -4.549783, TJ, TJ1, Result)
    Call UCheb(X, -1.195905, TJ, TJ1, Result)
    Call UCheb(X, -0.2733093, TJ, TJ1, Result)
    Call UCheb(X, -0.09428447, TJ, TJ1, Result)
    Call UCheb(X, -0.03760093, TJ, TJ1, Result)
    Call UCheb(X, -0.01431676, TJ, TJ1, Result)
    Call UCheb(X, -0.004717152, TJ, TJ1, Result)
    Call UCheb(X, -0.0001032199, TJ, TJ1, Result)
    Call UCheb(X, 0.001832423, TJ, TJ1, Result)
    Call UCheb(X, 0.002905979, TJ, TJ1, Result)
    Call UCheb(X, 0.003302799, TJ, TJ1, Result)
    Call UCheb(X, 0.003464371, TJ, TJ1, Result)
    Call UCheb(X, 0.003456211, TJ, TJ1, Result)
    Call UCheb(X, 0.002736244, TJ, TJ1, Result)
    Call UCheb(X, 0.002140712, TJ, TJ1, Result)
    UTblN7N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.5 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.235822, TJ, TJ1, Result)
    Call UCheb(X, -4.5641, TJ, TJ1, Result)
    Call UCheb(X, -1.190813, TJ, TJ1, Result)
    Call UCheb(X, -0.2686546, TJ, TJ1, Result)
    Call UCheb(X, -0.09395083, TJ, TJ1, Result)
    Call UCheb(X, -0.03967359, TJ, TJ1, Result)
    Call UCheb(X, -0.01747096, TJ, TJ1, Result)
    Call UCheb(X, -0.008304144, TJ, TJ1, Result)
    Call UCheb(X, -0.003903198, TJ, TJ1, Result)
    Call UCheb(X, -0.002134906, TJ, TJ1, Result)
    Call UCheb(X, -0.001175035, TJ, TJ1, Result)
    Call UCheb(X, -0.0007266224, TJ, TJ1, Result)
    Call UCheb(X, -0.0001892931, TJ, TJ1, Result)
    Call UCheb(X, 0.0005604706, TJ, TJ1, Result)
    Call UCheb(X, 0.0009070459, TJ, TJ1, Result)
    Call UCheb(X, 0.00142701, TJ, TJ1, Result)
    UTblN7N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.5 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.222204, TJ, TJ1, Result)
    Call UCheb(X, -4.5323, TJ, TJ1, Result)
    Call UCheb(X, -1.164642, TJ, TJ1, Result)
    Call UCheb(X, -0.2523768, TJ, TJ1, Result)
    Call UCheb(X, -0.08531984, TJ, TJ1, Result)
    Call UCheb(X, -0.03467857, TJ, TJ1, Result)
    Call UCheb(X, -0.01483804, TJ, TJ1, Result)
    Call UCheb(X, -0.006524136, TJ, TJ1, Result)
    Call UCheb(X, -0.00307774, TJ, TJ1, Result)
    Call UCheb(X, -0.001745218, TJ, TJ1, Result)
    Call UCheb(X, -0.001602085, TJ, TJ1, Result)
    Call UCheb(X, -0.001828831, TJ, TJ1, Result)
    Call UCheb(X, -0.00199407, TJ, TJ1, Result)
    Call UCheb(X, -0.001873879, TJ, TJ1, Result)
    Call UCheb(X, -0.001341937, TJ, TJ1, Result)
    Call UCheb(X, -0.0008706444, TJ, TJ1, Result)
    UTblN7N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.5 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.211763, TJ, TJ1, Result)
    Call UCheb(X, -4.507542, TJ, TJ1, Result)
    Call UCheb(X, -1.14364, TJ, TJ1, Result)
    Call UCheb(X, -0.2395755, TJ, TJ1, Result)
    Call UCheb(X, -0.0780802, TJ, TJ1, Result)
    Call UCheb(X, -0.03044259, TJ, TJ1, Result)
    Call UCheb(X, -0.01182308, TJ, TJ1, Result)
    Call UCheb(X, -0.004057325, TJ, TJ1, Result)
    Call UCheb(X, -0.0005724255, TJ, TJ1, Result)
    Call UCheb(X, 0.00083039, TJ, TJ1, Result)
    Call UCheb(X, 0.001113148, TJ, TJ1, Result)
    Call UCheb(X, 0.0008102514, TJ, TJ1, Result)
    Call UCheb(X, 0.0003559442, TJ, TJ1, Result)
    Call UCheb(X, 0.00004634986, TJ, TJ1, Result)
    Call UCheb(X, -0.00008776476, TJ, TJ1, Result)
    Call UCheb(X, 0.00001054489, TJ, TJ1, Result)
    UTblN7N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.5 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.204898, TJ, TJ1, Result)
    Call UCheb(X, -4.48996, TJ, TJ1, Result)
    Call UCheb(X, -1.129172, TJ, TJ1, Result)
    Call UCheb(X, -0.2316741, TJ, TJ1, Result)
    Call UCheb(X, -0.07506107, TJ, TJ1, Result)
    Call UCheb(X, -0.02983676, TJ, TJ1, Result)
    Call UCheb(X, -0.01258013, TJ, TJ1, Result)
    Call UCheb(X, -0.005262515, TJ, TJ1, Result)
    Call UCheb(X, -0.001984156, TJ, TJ1, Result)
    Call UCheb(X, -0.0003912108, TJ, TJ1, Result)
    Call UCheb(X, 0.00008974023, TJ, TJ1, Result)
    Call UCheb(X, 0.00006056195, TJ, TJ1, Result)
    Call UCheb(X, -0.0002090842, TJ, TJ1, Result)
    Call UCheb(X, -0.000523262, TJ, TJ1, Result)
    Call UCheb(X, -0.0005816339, TJ, TJ1, Result)
    Call UCheb(X, -0.0007020421, TJ, TJ1, Result)
    UTblN7N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.5 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.176536, TJ, TJ1, Result)
    Call UCheb(X, -4.398705, TJ, TJ1, Result)
    Call UCheb(X, -1.045481, TJ, TJ1, Result)
    Call UCheb(X, -0.1821982, TJ, TJ1, Result)
    Call UCheb(X, -0.04962304, TJ, TJ1, Result)
    Call UCheb(X, -0.01698132, TJ, TJ1, Result)
    Call UCheb(X, -0.006062667, TJ, TJ1, Result)
    Call UCheb(X, -0.002282353, TJ, TJ1, Result)
    Call UCheb(X, -0.0008014836, TJ, TJ1, Result)
    Call UCheb(X, -0.0002035683, TJ, TJ1, Result)
    Call UCheb(X, -0.00001004137, TJ, TJ1, Result)
    Call UCheb(X, 0.000003801453, TJ, TJ1, Result)
    Call UCheb(X, -0.00001920705, TJ, TJ1, Result)
    Call UCheb(X, -0.00002518735, TJ, TJ1, Result)
    Call UCheb(X, -0.00001821501, TJ, TJ1, Result)
    Call UCheb(X, -0.00001801008, TJ, TJ1, Result)
    UTblN7N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 7, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN7N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.5 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.188337, TJ, TJ1, Result)
    Call UCheb(X, -4.386949, TJ, TJ1, Result)
    Call UCheb(X, -1.022834, TJ, TJ1, Result)
    Call UCheb(X, -0.1686517, TJ, TJ1, Result)
    Call UCheb(X, -0.04323516, TJ, TJ1, Result)
    Call UCheb(X, -0.01399392, TJ, TJ1, Result)
    Call UCheb(X, -0.004644333, TJ, TJ1, Result)
    Call UCheb(X, -0.001617044, TJ, TJ1, Result)
    Call UCheb(X, -0.0005031396, TJ, TJ1, Result)
    Call UCheb(X, -0.00008792066, TJ, TJ1, Result)
    Call UCheb(X, 0.00002675457, TJ, TJ1, Result)
    Call UCheb(X, 0.00001673416, TJ, TJ1, Result)
    Call UCheb(X, -0.000006258552, TJ, TJ1, Result)
    Call UCheb(X, -0.000008174214, TJ, TJ1, Result)
    Call UCheb(X, -0.000003073644, TJ, TJ1, Result)
    Call UCheb(X, -0.000001349958, TJ, TJ1, Result)
    UTblN7N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 8)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N8(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.360672 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -3.940217, TJ, TJ1, Result)
    Call UCheb(X, -4.168913, TJ, TJ1, Result)
    Call UCheb(X, -1.051485, TJ, TJ1, Result)
    Call UCheb(X, -0.2195325, TJ, TJ1, Result)
    Call UCheb(X, -0.06775196, TJ, TJ1, Result)
    Call UCheb(X, -0.02385506, TJ, TJ1, Result)
    Call UCheb(X, -0.008244902, TJ, TJ1, Result)
    Call UCheb(X, -0.002525632, TJ, TJ1, Result)
    Call UCheb(X, 0.0002771275, TJ, TJ1, Result)
    Call UCheb(X, 0.002332874, TJ, TJ1, Result)
    Call UCheb(X, 0.004079599, TJ, TJ1, Result)
    Call UCheb(X, 0.004882551, TJ, TJ1, Result)
    Call UCheb(X, 0.004407944, TJ, TJ1, Result)
    Call UCheb(X, 0.002769844, TJ, TJ1, Result)
    Call UCheb(X, 0.001062433, TJ, TJ1, Result)
    Call UCheb(X, 0.00005872535, TJ, TJ1, Result)
    UTblN8N8 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 9)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N9(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.464102 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.147004, TJ, TJ1, Result)
    Call UCheb(X, -4.446939, TJ, TJ1, Result)
    Call UCheb(X, -1.146155, TJ, TJ1, Result)
    Call UCheb(X, -0.2488561, TJ, TJ1, Result)
    Call UCheb(X, -0.08144561, TJ, TJ1, Result)
    Call UCheb(X, -0.03116917, TJ, TJ1, Result)
    Call UCheb(X, -0.01205667, TJ, TJ1, Result)
    Call UCheb(X, -0.004515661, TJ, TJ1, Result)
    Call UCheb(X, -0.0007618616, TJ, TJ1, Result)
    Call UCheb(X, 0.001599011, TJ, TJ1, Result)
    Call UCheb(X, 0.003457324, TJ, TJ1, Result)
    Call UCheb(X, 0.004482917, TJ, TJ1, Result)
    Call UCheb(X, 0.004488267, TJ, TJ1, Result)
    Call UCheb(X, 0.003469823, TJ, TJ1, Result)
    Call UCheb(X, 0.001957591, TJ, TJ1, Result)
    Call UCheb(X, 0.0008058326, TJ, TJ1, Result)
    UTblN8N9 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 10)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N10(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.554093 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.334282, TJ, TJ1, Result)
    Call UCheb(X, -4.70086, TJ, TJ1, Result)
    Call UCheb(X, -1.235253, TJ, TJ1, Result)
    Call UCheb(X, -0.2778489, TJ, TJ1, Result)
    Call UCheb(X, -0.09527324, TJ, TJ1, Result)
    Call UCheb(X, -0.03862885, TJ, TJ1, Result)
    Call UCheb(X, -0.01589781, TJ, TJ1, Result)
    Call UCheb(X, -0.006507355, TJ, TJ1, Result)
    Call UCheb(X, -0.001717526, TJ, TJ1, Result)
    Call UCheb(X, 0.0009215726, TJ, TJ1, Result)
    Call UCheb(X, 0.002848696, TJ, TJ1, Result)
    Call UCheb(X, 0.003918854, TJ, TJ1, Result)
    Call UCheb(X, 0.004219614, TJ, TJ1, Result)
    Call UCheb(X, 0.003753761, TJ, TJ1, Result)
    Call UCheb(X, 0.002573688, TJ, TJ1, Result)
    Call UCheb(X, 0.001602177, TJ, TJ1, Result)
    UTblN8N10 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.421882, TJ, TJ1, Result)
    Call UCheb(X, -4.812457, TJ, TJ1, Result)
    Call UCheb(X, -1.266153, TJ, TJ1, Result)
    Call UCheb(X, -0.2849344, TJ, TJ1, Result)
    Call UCheb(X, -0.09971527, TJ, TJ1, Result)
    Call UCheb(X, -0.04258944, TJ, TJ1, Result)
    Call UCheb(X, -0.0194482, TJ, TJ1, Result)
    Call UCheb(X, -0.009894685, TJ, TJ1, Result)
    Call UCheb(X, -0.005031836, TJ, TJ1, Result)
    Call UCheb(X, -0.00251433, TJ, TJ1, Result)
    Call UCheb(X, -0.000635166, TJ, TJ1, Result)
    Call UCheb(X, 0.0006206748, TJ, TJ1, Result)
    Call UCheb(X, 0.0014926, TJ, TJ1, Result)
    Call UCheb(X, 0.002005338, TJ, TJ1, Result)
    Call UCheb(X, 0.001780099, TJ, TJ1, Result)
    Call UCheb(X, 0.001673599, TJ, TJ1, Result)
    UTblN8N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.398211, TJ, TJ1, Result)
    Call UCheb(X, -4.762214, TJ, TJ1, Result)
    Call UCheb(X, -1.226296, TJ, TJ1, Result)
    Call UCheb(X, -0.2603837, TJ, TJ1, Result)
    Call UCheb(X, -0.08643223, TJ, TJ1, Result)
    Call UCheb(X, -0.03502438, TJ, TJ1, Result)
    Call UCheb(X, -0.01544574, TJ, TJ1, Result)
    Call UCheb(X, -0.007647734, TJ, TJ1, Result)
    Call UCheb(X, -0.004442259, TJ, TJ1, Result)
    Call UCheb(X, -0.003011484, TJ, TJ1, Result)
    Call UCheb(X, -0.002384758, TJ, TJ1, Result)
    Call UCheb(X, -0.001998259, TJ, TJ1, Result)
    Call UCheb(X, -0.001659985, TJ, TJ1, Result)
    Call UCheb(X, -0.001331046, TJ, TJ1, Result)
    Call UCheb(X, -0.0008638478, TJ, TJ1, Result)
    Call UCheb(X, -0.0006056785, TJ, TJ1, Result)
    UTblN8N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.38067, TJ, TJ1, Result)
    Call UCheb(X, -4.724511, TJ, TJ1, Result)
    Call UCheb(X, -1.195851, TJ, TJ1, Result)
    Call UCheb(X, -0.2420511, TJ, TJ1, Result)
    Call UCheb(X, -0.07609928, TJ, TJ1, Result)
    Call UCheb(X, -0.02893999, TJ, TJ1, Result)
    Call UCheb(X, -0.01115919, TJ, TJ1, Result)
    Call UCheb(X, -0.00429141, TJ, TJ1, Result)
    Call UCheb(X, -0.001339664, TJ, TJ1, Result)
    Call UCheb(X, -0.0001801548, TJ, TJ1, Result)
    Call UCheb(X, 0.000253471, TJ, TJ1, Result)
    Call UCheb(X, 0.000279325, TJ, TJ1, Result)
    Call UCheb(X, 0.0001806718, TJ, TJ1, Result)
    Call UCheb(X, 0.0001384624, TJ, TJ1, Result)
    Call UCheb(X, 0.0001120582, TJ, TJ1, Result)
    Call UCheb(X, 0.0002936453, TJ, TJ1, Result)
    UTblN8N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.368494, TJ, TJ1, Result)
    Call UCheb(X, -4.697171, TJ, TJ1, Result)
    Call UCheb(X, -1.17444, TJ, TJ1, Result)
    Call UCheb(X, -0.2300621, TJ, TJ1, Result)
    Call UCheb(X, -0.07087393, TJ, TJ1, Result)
    Call UCheb(X, -0.02685826, TJ, TJ1, Result)
    Call UCheb(X, -0.01085254, TJ, TJ1, Result)
    Call UCheb(X, -0.004525658, TJ, TJ1, Result)
    Call UCheb(X, -0.001966647, TJ, TJ1, Result)
    Call UCheb(X, -0.0007453388, TJ, TJ1, Result)
    Call UCheb(X, -0.0003826066, TJ, TJ1, Result)
    Call UCheb(X, -0.0003501958, TJ, TJ1, Result)
    Call UCheb(X, -0.0005336297, TJ, TJ1, Result)
    Call UCheb(X, -0.0008251972, TJ, TJ1, Result)
    Call UCheb(X, -0.0008118456, TJ, TJ1, Result)
    Call UCheb(X, -0.0009415959, TJ, TJ1, Result)
    UTblN8N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.358397, TJ, TJ1, Result)
    Call UCheb(X, -4.674485, TJ, TJ1, Result)
    Call UCheb(X, -1.155941, TJ, TJ1, Result)
    Call UCheb(X, -0.219578, TJ, TJ1, Result)
    Call UCheb(X, -0.0654483, TJ, TJ1, Result)
    Call UCheb(X, -0.02426183, TJ, TJ1, Result)
    Call UCheb(X, -0.009309902, TJ, TJ1, Result)
    Call UCheb(X, -0.003650956, TJ, TJ1, Result)
    Call UCheb(X, -0.001068874, TJ, TJ1, Result)
    Call UCheb(X, 0.0001538544, TJ, TJ1, Result)
    Call UCheb(X, 0.0008192525, TJ, TJ1, Result)
    Call UCheb(X, 0.001073905, TJ, TJ1, Result)
    Call UCheb(X, 0.001079673, TJ, TJ1, Result)
    Call UCheb(X, 0.0009423572, TJ, TJ1, Result)
    Call UCheb(X, 0.0006579647, TJ, TJ1, Result)
    Call UCheb(X, 0.0004765904, TJ, TJ1, Result)
    UTblN8N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.318823, TJ, TJ1, Result)
    Call UCheb(X, -4.567159, TJ, TJ1, Result)
    Call UCheb(X, -1.064864, TJ, TJ1, Result)
    Call UCheb(X, -0.1688413, TJ, TJ1, Result)
    Call UCheb(X, -0.04153712, TJ, TJ1, Result)
    Call UCheb(X, -0.01309389, TJ, TJ1, Result)
    Call UCheb(X, -0.004226861, TJ, TJ1, Result)
    Call UCheb(X, -0.001523815, TJ, TJ1, Result)
    Call UCheb(X, -0.0005780987, TJ, TJ1, Result)
    Call UCheb(X, -0.0002166866, TJ, TJ1, Result)
    Call UCheb(X, -0.00006922431, TJ, TJ1, Result)
    Call UCheb(X, -0.00001466397, TJ, TJ1, Result)
    Call UCheb(X, -0.000005690036, TJ, TJ1, Result)
    Call UCheb(X, -0.00001008185, TJ, TJ1, Result)
    Call UCheb(X, -0.000009271903, TJ, TJ1, Result)
    Call UCheb(X, -0.000007534751, TJ, TJ1, Result)
    UTblN8N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 8, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN8N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.6 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.324531, TJ, TJ1, Result)
    Call UCheb(X, -4.547071, TJ, TJ1, Result)
    Call UCheb(X, -1.038129, TJ, TJ1, Result)
    Call UCheb(X, -0.1541549, TJ, TJ1, Result)
    Call UCheb(X, -0.03525605, TJ, TJ1, Result)
    Call UCheb(X, -0.01044992, TJ, TJ1, Result)
    Call UCheb(X, -0.003085713, TJ, TJ1, Result)
    Call UCheb(X, -0.001017871, TJ, TJ1, Result)
    Call UCheb(X, -0.0003459226, TJ, TJ1, Result)
    Call UCheb(X, -0.0001092064, TJ, TJ1, Result)
    Call UCheb(X, -0.00002024349, TJ, TJ1, Result)
    Call UCheb(X, 0.000007366347, TJ, TJ1, Result)
    Call UCheb(X, 0.000006385637, TJ, TJ1, Result)
    Call UCheb(X, 0.00000008321722, TJ, TJ1, Result)
    Call UCheb(X, -0.000001439286, TJ, TJ1, Result)
    Call UCheb(X, -0.0000003058079, TJ, TJ1, Result)
    UTblN8N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 9)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N9(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.576237 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.372857, TJ, TJ1, Result)
    Call UCheb(X, -4.750859, TJ, TJ1, Result)
    Call UCheb(X, -1.248233, TJ, TJ1, Result)
    Call UCheb(X, -0.2792868, TJ, TJ1, Result)
    Call UCheb(X, -0.09559372, TJ, TJ1, Result)
    Call UCheb(X, -0.03894941, TJ, TJ1, Result)
    Call UCheb(X, -0.01643256, TJ, TJ1, Result)
    Call UCheb(X, -0.00709137, TJ, TJ1, Result)
    Call UCheb(X, -0.002285034, TJ, TJ1, Result)
    Call UCheb(X, 0.0006112997, TJ, TJ1, Result)
    Call UCheb(X, 0.002806229, TJ, TJ1, Result)
    Call UCheb(X, 0.004150741, TJ, TJ1, Result)
    Call UCheb(X, 0.004509825, TJ, TJ1, Result)
    Call UCheb(X, 0.003891051, TJ, TJ1, Result)
    Call UCheb(X, 0.002485013, TJ, TJ1, Result)
    Call UCheb(X, 0.001343653, TJ, TJ1, Result)
    UTblN9N9 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 10)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N10(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.516726, TJ, TJ1, Result)
    Call UCheb(X, -4.939333, TJ, TJ1, Result)
    Call UCheb(X, -1.305046, TJ, TJ1, Result)
    Call UCheb(X, -0.2935326, TJ, TJ1, Result)
    Call UCheb(X, -0.1029141, TJ, TJ1, Result)
    Call UCheb(X, -0.04420592, TJ, TJ1, Result)
    Call UCheb(X, -0.0205314, TJ, TJ1, Result)
    Call UCheb(X, -0.0106593, TJ, TJ1, Result)
    Call UCheb(X, -0.005523581, TJ, TJ1, Result)
    Call UCheb(X, -0.002544888, TJ, TJ1, Result)
    Call UCheb(X, -0.0001813741, TJ, TJ1, Result)
    Call UCheb(X, 0.001510631, TJ, TJ1, Result)
    Call UCheb(X, 0.002536057, TJ, TJ1, Result)
    Call UCheb(X, 0.002833815, TJ, TJ1, Result)
    Call UCheb(X, 0.002189692, TJ, TJ1, Result)
    Call UCheb(X, 0.00161505, TJ, TJ1, Result)
    UTblN9N10 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.481308, TJ, TJ1, Result)
    Call UCheb(X, -4.867483, TJ, TJ1, Result)
    Call UCheb(X, -1.249072, TJ, TJ1, Result)
    Call UCheb(X, -0.259179, TJ, TJ1, Result)
    Call UCheb(X, -0.08400128, TJ, TJ1, Result)
    Call UCheb(X, -0.03341992, TJ, TJ1, Result)
    Call UCheb(X, -0.0146368, TJ, TJ1, Result)
    Call UCheb(X, -0.007487211, TJ, TJ1, Result)
    Call UCheb(X, -0.004671196, TJ, TJ1, Result)
    Call UCheb(X, -0.003343472, TJ, TJ1, Result)
    Call UCheb(X, -0.002544146, TJ, TJ1, Result)
    Call UCheb(X, -0.001802335, TJ, TJ1, Result)
    Call UCheb(X, -0.001117084, TJ, TJ1, Result)
    Call UCheb(X, -0.0006217443, TJ, TJ1, Result)
    Call UCheb(X, -0.0002858766, TJ, TJ1, Result)
    Call UCheb(X, -0.0003193687, TJ, TJ1, Result)
    UTblN9N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.456776, TJ, TJ1, Result)
    Call UCheb(X, -4.817037, TJ, TJ1, Result)
    Call UCheb(X, -1.209788, TJ, TJ1, Result)
    Call UCheb(X, -0.2362108, TJ, TJ1, Result)
    Call UCheb(X, -0.07171356, TJ, TJ1, Result)
    Call UCheb(X, -0.02661557, TJ, TJ1, Result)
    Call UCheb(X, -0.01026141, TJ, TJ1, Result)
    Call UCheb(X, -0.004361908, TJ, TJ1, Result)
    Call UCheb(X, -0.002093885, TJ, TJ1, Result)
    Call UCheb(X, -0.001298389, TJ, TJ1, Result)
    Call UCheb(X, -0.0009663603, TJ, TJ1, Result)
    Call UCheb(X, -0.0007768522, TJ, TJ1, Result)
    Call UCheb(X, -0.0005579015, TJ, TJ1, Result)
    Call UCheb(X, -0.0002868677, TJ, TJ1, Result)
    Call UCheb(X, -0.00007440652, TJ, TJ1, Result)
    Call UCheb(X, 0.0001523037, TJ, TJ1, Result)
    UTblN9N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.43884, TJ, TJ1, Result)
    Call UCheb(X, -4.779308, TJ, TJ1, Result)
    Call UCheb(X, -1.180614, TJ, TJ1, Result)
    Call UCheb(X, -0.2196489, TJ, TJ1, Result)
    Call UCheb(X, -0.06346621, TJ, TJ1, Result)
    Call UCheb(X, -0.02234857, TJ, TJ1, Result)
    Call UCheb(X, -0.007796211, TJ, TJ1, Result)
    Call UCheb(X, -0.002575715, TJ, TJ1, Result)
    Call UCheb(X, -0.0005525647, TJ, TJ1, Result)
    Call UCheb(X, 0.0001964651, TJ, TJ1, Result)
    Call UCheb(X, 0.0004275235, TJ, TJ1, Result)
    Call UCheb(X, 0.0004299124, TJ, TJ1, Result)
    Call UCheb(X, 0.0003397416, TJ, TJ1, Result)
    Call UCheb(X, 0.0002295781, TJ, TJ1, Result)
    Call UCheb(X, 0.0001237619, TJ, TJ1, Result)
    Call UCheb(X, 0.00007269692, TJ, TJ1, Result)
    UTblN9N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.425981, TJ, TJ1, Result)
    Call UCheb(X, -4.751545, TJ, TJ1, Result)
    Call UCheb(X, -1.159543, TJ, TJ1, Result)
    Call UCheb(X, -0.208657, TJ, TJ1, Result)
    Call UCheb(X, -0.05917446, TJ, TJ1, Result)
    Call UCheb(X, -0.02120112, TJ, TJ1, Result)
    Call UCheb(X, -0.008175519, TJ, TJ1, Result)
    Call UCheb(X, -0.003515473, TJ, TJ1, Result)
    Call UCheb(X, -0.001727772, TJ, TJ1, Result)
    Call UCheb(X, -0.0009070629, TJ, TJ1, Result)
    Call UCheb(X, -0.0005677569, TJ, TJ1, Result)
    Call UCheb(X, -0.0003876953, TJ, TJ1, Result)
    Call UCheb(X, -0.0003233502, TJ, TJ1, Result)
    Call UCheb(X, -0.0003508182, TJ, TJ1, Result)
    Call UCheb(X, -0.0003120389, TJ, TJ1, Result)
    Call UCheb(X, -0.0003847212, TJ, TJ1, Result)
    UTblN9N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.414952, TJ, TJ1, Result)
    Call UCheb(X, -4.727612, TJ, TJ1, Result)
    Call UCheb(X, -1.140634, TJ, TJ1, Result)
    Call UCheb(X, -0.1981231, TJ, TJ1, Result)
    Call UCheb(X, -0.05382635, TJ, TJ1, Result)
    Call UCheb(X, -0.01853575, TJ, TJ1, Result)
    Call UCheb(X, -0.006571051, TJ, TJ1, Result)
    Call UCheb(X, -0.002567625, TJ, TJ1, Result)
    Call UCheb(X, -0.0009214197, TJ, TJ1, Result)
    Call UCheb(X, -0.00024487, TJ, TJ1, Result)
    Call UCheb(X, 0.0001712669, TJ, TJ1, Result)
    Call UCheb(X, 0.000401505, TJ, TJ1, Result)
    Call UCheb(X, 0.000543861, TJ, TJ1, Result)
    Call UCheb(X, 0.0006301363, TJ, TJ1, Result)
    Call UCheb(X, 0.0005309386, TJ, TJ1, Result)
    Call UCheb(X, 0.0005164772, TJ, TJ1, Result)
    UTblN9N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.37072, TJ, TJ1, Result)
    Call UCheb(X, -4.615712, TJ, TJ1, Result)
    Call UCheb(X, -1.050023, TJ, TJ1, Result)
    Call UCheb(X, -0.1504775, TJ, TJ1, Result)
    Call UCheb(X, -0.03318265, TJ, TJ1, Result)
    Call UCheb(X, -0.009646826, TJ, TJ1, Result)
    Call UCheb(X, -0.002741492, TJ, TJ1, Result)
    Call UCheb(X, -0.000873536, TJ, TJ1, Result)
    Call UCheb(X, -0.0002966911, TJ, TJ1, Result)
    Call UCheb(X, -0.0001100738, TJ, TJ1, Result)
    Call UCheb(X, -0.00004348991, TJ, TJ1, Result)
    Call UCheb(X, -0.00001527687, TJ, TJ1, Result)
    Call UCheb(X, -0.000002917286, TJ, TJ1, Result)
    Call UCheb(X, 0.0000003397466, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002360175, TJ, TJ1, Result)
    Call UCheb(X, -0.0000009892252, TJ, TJ1, Result)
    UTblN9N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 9, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN9N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.372506, TJ, TJ1, Result)
    Call UCheb(X, -4.590966, TJ, TJ1, Result)
    Call UCheb(X, -1.021758, TJ, TJ1, Result)
    Call UCheb(X, -0.1359849, TJ, TJ1, Result)
    Call UCheb(X, -0.02755519, TJ, TJ1, Result)
    Call UCheb(X, -0.007533166, TJ, TJ1, Result)
    Call UCheb(X, -0.001936659, TJ, TJ1, Result)
    Call UCheb(X, -0.0005634913, TJ, TJ1, Result)
    Call UCheb(X, -0.0001730053, TJ, TJ1, Result)
    Call UCheb(X, -0.00005791845, TJ, TJ1, Result)
    Call UCheb(X, -0.00002030682, TJ, TJ1, Result)
    Call UCheb(X, -0.000005228663, TJ, TJ1, Result)
    Call UCheb(X, 0.0000008631175, TJ, TJ1, Result)
    Call UCheb(X, 0.000001636749, TJ, TJ1, Result)
    Call UCheb(X, 0.0000004404599, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002789872, TJ, TJ1, Result)
    UTblN9N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 10)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N10(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.468831, TJ, TJ1, Result)
    Call UCheb(X, -4.844398, TJ, TJ1, Result)
    Call UCheb(X, -1.231728, TJ, TJ1, Result)
    Call UCheb(X, -0.2486073, TJ, TJ1, Result)
    Call UCheb(X, -0.07781321, TJ, TJ1, Result)
    Call UCheb(X, -0.02971425, TJ, TJ1, Result)
    Call UCheb(X, -0.01215371, TJ, TJ1, Result)
    Call UCheb(X, -0.005828451, TJ, TJ1, Result)
    Call UCheb(X, -0.003419872, TJ, TJ1, Result)
    Call UCheb(X, -0.002430165, TJ, TJ1, Result)
    Call UCheb(X, -0.001740363, TJ, TJ1, Result)
    Call UCheb(X, -0.001049211, TJ, TJ1, Result)
    Call UCheb(X, -0.0003269371, TJ, TJ1, Result)
    Call UCheb(X, 0.0002211393, TJ, TJ1, Result)
    Call UCheb(X, 0.0004232314, TJ, TJ1, Result)
    Call UCheb(X, 0.0003016081, TJ, TJ1, Result)
    UTblN10N10 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.437998, TJ, TJ1, Result)
    Call UCheb(X, -4.782296, TJ, TJ1, Result)
    Call UCheb(X, -1.184732, TJ, TJ1, Result)
    Call UCheb(X, -0.2219585, TJ, TJ1, Result)
    Call UCheb(X, -0.06457012, TJ, TJ1, Result)
    Call UCheb(X, -0.02296008, TJ, TJ1, Result)
    Call UCheb(X, -0.008481501, TJ, TJ1, Result)
    Call UCheb(X, -0.00352794, TJ, TJ1, Result)
    Call UCheb(X, -0.001953426, TJ, TJ1, Result)
    Call UCheb(X, -0.00156384, TJ, TJ1, Result)
    Call UCheb(X, -0.001574403, TJ, TJ1, Result)
    Call UCheb(X, -0.001535775, TJ, TJ1, Result)
    Call UCheb(X, -0.001338037, TJ, TJ1, Result)
    Call UCheb(X, -0.001002654, TJ, TJ1, Result)
    Call UCheb(X, -0.0005852676, TJ, TJ1, Result)
    Call UCheb(X, -0.0003318132, TJ, TJ1, Result)
    UTblN10N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.416082, TJ, TJ1, Result)
    Call UCheb(X, -4.737458, TJ, TJ1, Result)
    Call UCheb(X, -1.150952, TJ, TJ1, Result)
    Call UCheb(X, -0.2036884, TJ, TJ1, Result)
    Call UCheb(X, -0.0560903, TJ, TJ1, Result)
    Call UCheb(X, -0.01908684, TJ, TJ1, Result)
    Call UCheb(X, -0.006439666, TJ, TJ1, Result)
    Call UCheb(X, -0.002162647, TJ, TJ1, Result)
    Call UCheb(X, -0.0006451601, TJ, TJ1, Result)
    Call UCheb(X, -0.0002148757, TJ, TJ1, Result)
    Call UCheb(X, -0.0001803981, TJ, TJ1, Result)
    Call UCheb(X, -0.0002731621, TJ, TJ1, Result)
    Call UCheb(X, -0.0003346903, TJ, TJ1, Result)
    Call UCheb(X, -0.0003013151, TJ, TJ1, Result)
    Call UCheb(X, -0.0001956148, TJ, TJ1, Result)
    Call UCheb(X, -0.00002438381, TJ, TJ1, Result)
    UTblN10N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.39948, TJ, TJ1, Result)
    Call UCheb(X, -4.702863, TJ, TJ1, Result)
    Call UCheb(X, -1.124829, TJ, TJ1, Result)
    Call UCheb(X, -0.1897428, TJ, TJ1, Result)
    Call UCheb(X, -0.04979802, TJ, TJ1, Result)
    Call UCheb(X, -0.01634368, TJ, TJ1, Result)
    Call UCheb(X, -0.005180461, TJ, TJ1, Result)
    Call UCheb(X, -0.001484926, TJ, TJ1, Result)
    Call UCheb(X, -0.00007864376, TJ, TJ1, Result)
    Call UCheb(X, 0.0004186576, TJ, TJ1, Result)
    Call UCheb(X, 0.0005886925, TJ, TJ1, Result)
    Call UCheb(X, 0.0005836828, TJ, TJ1, Result)
    Call UCheb(X, 0.0005074756, TJ, TJ1, Result)
    Call UCheb(X, 0.0004209547, TJ, TJ1, Result)
    Call UCheb(X, 0.0002883266, TJ, TJ1, Result)
    Call UCheb(X, 0.0002380143, TJ, TJ1, Result)
    UTblN10N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.386924, TJ, TJ1, Result)
    Call UCheb(X, -4.676124, TJ, TJ1, Result)
    Call UCheb(X, -1.10474, TJ, TJ1, Result)
    Call UCheb(X, -0.1793826, TJ, TJ1, Result)
    Call UCheb(X, -0.04558886, TJ, TJ1, Result)
    Call UCheb(X, -0.01492462, TJ, TJ1, Result)
    Call UCheb(X, -0.005052903, TJ, TJ1, Result)
    Call UCheb(X, -0.001917782, TJ, TJ1, Result)
    Call UCheb(X, -0.0007878696, TJ, TJ1, Result)
    Call UCheb(X, -0.0003576046, TJ, TJ1, Result)
    Call UCheb(X, -0.0001764551, TJ, TJ1, Result)
    Call UCheb(X, -0.00009288778, TJ, TJ1, Result)
    Call UCheb(X, -0.00004757658, TJ, TJ1, Result)
    Call UCheb(X, -0.00002299101, TJ, TJ1, Result)
    Call UCheb(X, -0.000009265197, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002384503, TJ, TJ1, Result)
    UTblN10N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.376846, TJ, TJ1, Result)
    Call UCheb(X, -4.654247, TJ, TJ1, Result)
    Call UCheb(X, -1.088083, TJ, TJ1, Result)
    Call UCheb(X, -0.1705945, TJ, TJ1, Result)
    Call UCheb(X, -0.04169677, TJ, TJ1, Result)
    Call UCheb(X, -0.01317213, TJ, TJ1, Result)
    Call UCheb(X, -0.004264836, TJ, TJ1, Result)
    Call UCheb(X, -0.001548024, TJ, TJ1, Result)
    Call UCheb(X, -0.000663391, TJ, TJ1, Result)
    Call UCheb(X, -0.0003505621, TJ, TJ1, Result)
    Call UCheb(X, -0.0002658588, TJ, TJ1, Result)
    Call UCheb(X, -0.0002320254, TJ, TJ1, Result)
    Call UCheb(X, -0.0002175277, TJ, TJ1, Result)
    Call UCheb(X, -0.0002122317, TJ, TJ1, Result)
    Call UCheb(X, -0.0001675688, TJ, TJ1, Result)
    Call UCheb(X, -0.0001661363, TJ, TJ1, Result)
    UTblN10N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.333977, TJ, TJ1, Result)
    Call UCheb(X, -4.548099, TJ, TJ1, Result)
    Call UCheb(X, -1.004444, TJ, TJ1, Result)
    Call UCheb(X, -0.1291014, TJ, TJ1, Result)
    Call UCheb(X, -0.02523674, TJ, TJ1, Result)
    Call UCheb(X, -0.006828211, TJ, TJ1, Result)
    Call UCheb(X, -0.001716917, TJ, TJ1, Result)
    Call UCheb(X, -0.0004894256, TJ, TJ1, Result)
    Call UCheb(X, -0.0001433371, TJ, TJ1, Result)
    Call UCheb(X, -0.00004522675, TJ, TJ1, Result)
    Call UCheb(X, -0.00001764192, TJ, TJ1, Result)
    Call UCheb(X, -0.000009140235, TJ, TJ1, Result)
    Call UCheb(X, -0.00000562923, TJ, TJ1, Result)
    Call UCheb(X, -0.000003541895, TJ, TJ1, Result)
    Call UCheb(X, -0.000001944946, TJ, TJ1, Result)
    Call UCheb(X, -0.00000172636, TJ, TJ1, Result)
    UTblN10N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 10, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN10N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.65 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.334008, TJ, TJ1, Result)
    Call UCheb(X, -4.522316, TJ, TJ1, Result)
    Call UCheb(X, -0.9769627, TJ, TJ1, Result)
    Call UCheb(X, -0.115811, TJ, TJ1, Result)
    Call UCheb(X, -0.0205365, TJ, TJ1, Result)
    Call UCheb(X, -0.005242235, TJ, TJ1, Result)
    Call UCheb(X, -0.001173571, TJ, TJ1, Result)
    Call UCheb(X, -0.0003033661, TJ, TJ1, Result)
    Call UCheb(X, -0.00007824732, TJ, TJ1, Result)
    Call UCheb(X, -0.0000208442, TJ, TJ1, Result)
    Call UCheb(X, -0.000006610036, TJ, TJ1, Result)
    Call UCheb(X, -0.000002728155, TJ, TJ1, Result)
    Call UCheb(X, -0.00000121713, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002340966, TJ, TJ1, Result)
    Call UCheb(X, 0.0000002001235, TJ, TJ1, Result)
    Call UCheb(X, 0.0000001694052, TJ, TJ1, Result)
    UTblN10N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 11)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N11(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.51976, TJ, TJ1, Result)
    Call UCheb(X, -4.880694, TJ, TJ1, Result)
    Call UCheb(X, -1.200698, TJ, TJ1, Result)
    Call UCheb(X, -0.2174092, TJ, TJ1, Result)
    Call UCheb(X, -0.06072304, TJ, TJ1, Result)
    Call UCheb(X, -0.02054773, TJ, TJ1, Result)
    Call UCheb(X, -0.006506613, TJ, TJ1, Result)
    Call UCheb(X, -0.001813942, TJ, TJ1, Result)
    Call UCheb(X, -0.0001223644, TJ, TJ1, Result)
    Call UCheb(X, 0.0002417416, TJ, TJ1, Result)
    Call UCheb(X, 0.0002499166, TJ, TJ1, Result)
    Call UCheb(X, 0.0001194332, TJ, TJ1, Result)
    Call UCheb(X, 0.00007369096, TJ, TJ1, Result)
    Call UCheb(X, 0.000196859, TJ, TJ1, Result)
    Call UCheb(X, 0.0002630532, TJ, TJ1, Result)
    Call UCheb(X, 0.0005061, TJ, TJ1, Result)
    UTblN11N11 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.49579, TJ, TJ1, Result)
    Call UCheb(X, -4.832622, TJ, TJ1, Result)
    Call UCheb(X, -1.16542, TJ, TJ1, Result)
    Call UCheb(X, -0.1987306, TJ, TJ1, Result)
    Call UCheb(X, -0.05265621, TJ, TJ1, Result)
    Call UCheb(X, -0.01723537, TJ, TJ1, Result)
    Call UCheb(X, -0.005347406, TJ, TJ1, Result)
    Call UCheb(X, -0.001353464, TJ, TJ1, Result)
    Call UCheb(X, 0.00006613369, TJ, TJ1, Result)
    Call UCheb(X, 0.0005102522, TJ, TJ1, Result)
    Call UCheb(X, 0.0005237709, TJ, TJ1, Result)
    Call UCheb(X, 0.0003665652, TJ, TJ1, Result)
    Call UCheb(X, 0.0001626903, TJ, TJ1, Result)
    Call UCheb(X, -0.00001167518, TJ, TJ1, Result)
    Call UCheb(X, -0.00008564455, TJ, TJ1, Result)
    Call UCheb(X, -0.000104732, TJ, TJ1, Result)
    UTblN11N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.47788, TJ, TJ1, Result)
    Call UCheb(X, -4.796242, TJ, TJ1, Result)
    Call UCheb(X, -1.138769, TJ, TJ1, Result)
    Call UCheb(X, -0.1851739, TJ, TJ1, Result)
    Call UCheb(X, -0.04722104, TJ, TJ1, Result)
    Call UCheb(X, -0.01548304, TJ, TJ1, Result)
    Call UCheb(X, -0.005176683, TJ, TJ1, Result)
    Call UCheb(X, -0.001817895, TJ, TJ1, Result)
    Call UCheb(X, -0.0005842451, TJ, TJ1, Result)
    Call UCheb(X, -0.0000893587, TJ, TJ1, Result)
    Call UCheb(X, 0.00008421777, TJ, TJ1, Result)
    Call UCheb(X, 0.0001238831, TJ, TJ1, Result)
    Call UCheb(X, 0.00008867026, TJ, TJ1, Result)
    Call UCheb(X, 0.00001458255, TJ, TJ1, Result)
    Call UCheb(X, -0.00003306259, TJ, TJ1, Result)
    Call UCheb(X, -0.00008961487, TJ, TJ1, Result)
    UTblN11N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.463683, TJ, TJ1, Result)
    Call UCheb(X, -4.766969, TJ, TJ1, Result)
    Call UCheb(X, -1.117082, TJ, TJ1, Result)
    Call UCheb(X, -0.1739574, TJ, TJ1, Result)
    Call UCheb(X, -0.04238865, TJ, TJ1, Result)
    Call UCheb(X, -0.01350306, TJ, TJ1, Result)
    Call UCheb(X, -0.004425871, TJ, TJ1, Result)
    Call UCheb(X, -0.001640172, TJ, TJ1, Result)
    Call UCheb(X, -0.0006660633, TJ, TJ1, Result)
    Call UCheb(X, -0.0002879883, TJ, TJ1, Result)
    Call UCheb(X, -0.0001349658, TJ, TJ1, Result)
    Call UCheb(X, -0.00006271795, TJ, TJ1, Result)
    Call UCheb(X, -0.00003304544, TJ, TJ1, Result)
    Call UCheb(X, -0.00003024201, TJ, TJ1, Result)
    Call UCheb(X, -0.00002816867, TJ, TJ1, Result)
    Call UCheb(X, -0.00004596787, TJ, TJ1, Result)
    UTblN11N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.452526, TJ, TJ1, Result)
    Call UCheb(X, -4.74357, TJ, TJ1, Result)
    Call UCheb(X, -1.099705, TJ, TJ1, Result)
    Call UCheb(X, -0.1650612, TJ, TJ1, Result)
    Call UCheb(X, -0.03858285, TJ, TJ1, Result)
    Call UCheb(X, -0.01187036, TJ, TJ1, Result)
    Call UCheb(X, -0.003689241, TJ, TJ1, Result)
    Call UCheb(X, -0.00129436, TJ, TJ1, Result)
    Call UCheb(X, -0.0005072623, TJ, TJ1, Result)
    Call UCheb(X, -0.0002278008, TJ, TJ1, Result)
    Call UCheb(X, -0.0001322382, TJ, TJ1, Result)
    Call UCheb(X, -0.00009131558, TJ, TJ1, Result)
    Call UCheb(X, -0.00007305669, TJ, TJ1, Result)
    Call UCheb(X, -0.00006825627, TJ, TJ1, Result)
    Call UCheb(X, -0.00005332689, TJ, TJ1, Result)
    Call UCheb(X, -0.00006120973, TJ, TJ1, Result)
    UTblN11N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.402621, TJ, TJ1, Result)
    Call UCheb(X, -4.62744, TJ, TJ1, Result)
    Call UCheb(X, -1.011333, TJ, TJ1, Result)
    Call UCheb(X, -0.1224126, TJ, TJ1, Result)
    Call UCheb(X, -0.02232856, TJ, TJ1, Result)
    Call UCheb(X, -0.005859347, TJ, TJ1, Result)
    Call UCheb(X, -0.001377381, TJ, TJ1, Result)
    Call UCheb(X, -0.0003756709, TJ, TJ1, Result)
    Call UCheb(X, -0.000103323, TJ, TJ1, Result)
    Call UCheb(X, -0.00002875472, TJ, TJ1, Result)
    Call UCheb(X, -0.000008608399, TJ, TJ1, Result)
    Call UCheb(X, -0.000003102943, TJ, TJ1, Result)
    Call UCheb(X, -0.000001740693, TJ, TJ1, Result)
    Call UCheb(X, -0.000001343139, TJ, TJ1, Result)
    Call UCheb(X, -0.0000009196878, TJ, TJ1, Result)
    Call UCheb(X, -0.0000006658062, TJ, TJ1, Result)
    UTblN11N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 11, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN11N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.398795, TJ, TJ1, Result)
    Call UCheb(X, -4.596486, TJ, TJ1, Result)
    Call UCheb(X, -0.9814761, TJ, TJ1, Result)
    Call UCheb(X, -0.1085187, TJ, TJ1, Result)
    Call UCheb(X, -0.01766529, TJ, TJ1, Result)
    Call UCheb(X, -0.004379425, TJ, TJ1, Result)
    Call UCheb(X, -0.0008986351, TJ, TJ1, Result)
    Call UCheb(X, -0.0002214705, TJ, TJ1, Result)
    Call UCheb(X, -0.00005360075, TJ, TJ1, Result)
    Call UCheb(X, -0.00001260869, TJ, TJ1, Result)
    Call UCheb(X, -0.000003033307, TJ, TJ1, Result)
    Call UCheb(X, -0.0000007727087, TJ, TJ1, Result)
    Call UCheb(X, -0.0000003393883, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002242989, TJ, TJ1, Result)
    Call UCheb(X, -0.0000001111928, TJ, TJ1, Result)
    Call UCheb(X, 0.000000003898823, TJ, TJ1, Result)
    UTblN11N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 12, 12)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN12N12(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.472616, TJ, TJ1, Result)
    Call UCheb(X, -4.786627, TJ, TJ1, Result)
    Call UCheb(X, -1.132099, TJ, TJ1, Result)
    Call UCheb(X, -0.1817523, TJ, TJ1, Result)
    Call UCheb(X, -0.04570179, TJ, TJ1, Result)
    Call UCheb(X, -0.01479511, TJ, TJ1, Result)
    Call UCheb(X, -0.004799492, TJ, TJ1, Result)
    Call UCheb(X, -0.00156535, TJ, TJ1, Result)
    Call UCheb(X, -0.0003530139, TJ, TJ1, Result)
    Call UCheb(X, 0.0001380132, TJ, TJ1, Result)
    Call UCheb(X, 0.0003242761, TJ, TJ1, Result)
    Call UCheb(X, 0.0003576269, TJ, TJ1, Result)
    Call UCheb(X, 0.0003018771, TJ, TJ1, Result)
    Call UCheb(X, 0.0001933911, TJ, TJ1, Result)
    Call UCheb(X, 0.00009002799, TJ, TJ1, Result)
    Call UCheb(X, -0.000002022048, TJ, TJ1, Result)
    UTblN12N12 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 12, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN12N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.4548, TJ, TJ1, Result)
    Call UCheb(X, -4.750794, TJ, TJ1, Result)
    Call UCheb(X, -1.105988, TJ, TJ1, Result)
    Call UCheb(X, -0.1684754, TJ, TJ1, Result)
    Call UCheb(X, -0.04011826, TJ, TJ1, Result)
    Call UCheb(X, -0.01262579, TJ, TJ1, Result)
    Call UCheb(X, -0.004044492, TJ, TJ1, Result)
    Call UCheb(X, -0.001478741, TJ, TJ1, Result)
    Call UCheb(X, -0.0005322165, TJ, TJ1, Result)
    Call UCheb(X, -0.0001621104, TJ, TJ1, Result)
    Call UCheb(X, 0.00004068753, TJ, TJ1, Result)
    Call UCheb(X, 0.0001468396, TJ, TJ1, Result)
    Call UCheb(X, 0.0002056235, TJ, TJ1, Result)
    Call UCheb(X, 0.0002327375, TJ, TJ1, Result)
    Call UCheb(X, 0.0001914877, TJ, TJ1, Result)
    Call UCheb(X, 0.0001784191, TJ, TJ1, Result)
    UTblN12N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 12, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN12N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.44091, TJ, TJ1, Result)
    Call UCheb(X, -4.722404, TJ, TJ1, Result)
    Call UCheb(X, -1.085254, TJ, TJ1, Result)
    Call UCheb(X, -0.1579439, TJ, TJ1, Result)
    Call UCheb(X, -0.03563738, TJ, TJ1, Result)
    Call UCheb(X, -0.0106673, TJ, TJ1, Result)
    Call UCheb(X, -0.003129346, TJ, TJ1, Result)
    Call UCheb(X, -0.001014531, TJ, TJ1, Result)
    Call UCheb(X, -0.0003129679, TJ, TJ1, Result)
    Call UCheb(X, -0.00008000909, TJ, TJ1, Result)
    Call UCheb(X, 0.00001996174, TJ, TJ1, Result)
    Call UCheb(X, 0.00006377924, TJ, TJ1, Result)
    Call UCheb(X, 0.00008936304, TJ, TJ1, Result)
    Call UCheb(X, 0.0001051098, TJ, TJ1, Result)
    Call UCheb(X, 0.0000902582, TJ, TJ1, Result)
    Call UCheb(X, 0.00008730585, TJ, TJ1, Result)
    UTblN12N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 12, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN12N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.430123, TJ, TJ1, Result)
    Call UCheb(X, -4.700008, TJ, TJ1, Result)
    Call UCheb(X, -1.068971, TJ, TJ1, Result)
    Call UCheb(X, -0.1499725, TJ, TJ1, Result)
    Call UCheb(X, -0.03250897, TJ, TJ1, Result)
    Call UCheb(X, -0.009473145, TJ, TJ1, Result)
    Call UCheb(X, -0.002680008, TJ, TJ1, Result)
    Call UCheb(X, -0.000848335, TJ, TJ1, Result)
    Call UCheb(X, -0.0002766992, TJ, TJ1, Result)
    Call UCheb(X, -0.00009891081, TJ, TJ1, Result)
    Call UCheb(X, -0.0000401514, TJ, TJ1, Result)
    Call UCheb(X, -0.00001977756, TJ, TJ1, Result)
    Call UCheb(X, -0.000008707414, TJ, TJ1, Result)
    Call UCheb(X, 0.000001114786, TJ, TJ1, Result)
    Call UCheb(X, 0.000006238865, TJ, TJ1, Result)
    Call UCheb(X, 0.00001381445, TJ, TJ1, Result)
    UTblN12N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 12, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN12N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.380023, TJ, TJ1, Result)
    Call UCheb(X, -4.585782, TJ, TJ1, Result)
    Call UCheb(X, -0.9838583, TJ, TJ1, Result)
    Call UCheb(X, -0.1103394, TJ, TJ1, Result)
    Call UCheb(X, -0.01834015, TJ, TJ1, Result)
    Call UCheb(X, -0.004635212, TJ, TJ1, Result)
    Call UCheb(X, -0.0009948212, TJ, TJ1, Result)
    Call UCheb(X, -0.0002574169, TJ, TJ1, Result)
    Call UCheb(X, -0.0000674798, TJ, TJ1, Result)
    Call UCheb(X, -0.00001833672, TJ, TJ1, Result)
    Call UCheb(X, -0.000005722433, TJ, TJ1, Result)
    Call UCheb(X, -0.000002181038, TJ, TJ1, Result)
    Call UCheb(X, -0.000001206473, TJ, TJ1, Result)
    Call UCheb(X, -0.0000009716003, TJ, TJ1, Result)
    Call UCheb(X, -0.0000007476434, TJ, TJ1, Result)
    Call UCheb(X, -0.00000072177, TJ, TJ1, Result)
    UTblN12N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 12, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN12N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.7 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.374567, TJ, TJ1, Result)
    Call UCheb(X, -4.553481, TJ, TJ1, Result)
    Call UCheb(X, -0.9541334, TJ, TJ1, Result)
    Call UCheb(X, -0.09701907, TJ, TJ1, Result)
    Call UCheb(X, -0.01414757, TJ, TJ1, Result)
    Call UCheb(X, -0.003404103, TJ, TJ1, Result)
    Call UCheb(X, -0.0006234388, TJ, TJ1, Result)
    Call UCheb(X, -0.0001453762, TJ, TJ1, Result)
    Call UCheb(X, -0.0000331106, TJ, TJ1, Result)
    Call UCheb(X, -0.000007317501, TJ, TJ1, Result)
    Call UCheb(X, -0.000001713888, TJ, TJ1, Result)
    Call UCheb(X, -0.0000003309583, TJ, TJ1, Result)
    Call UCheb(X, -0.00000004019804, TJ, TJ1, Result)
    Call UCheb(X, 0.000000001224829, TJ, TJ1, Result)
    Call UCheb(X, -0.00000001349019, TJ, TJ1, Result)
    Call UCheb(X, -0.00000001893302, TJ, TJ1, Result)
    UTblN12N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 13, 13)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN13N13(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.541046, TJ, TJ1, Result)
    Call UCheb(X, -4.859047, TJ, TJ1, Result)
    Call UCheb(X, -1.130164, TJ, TJ1, Result)
    Call UCheb(X, -0.1689719, TJ, TJ1, Result)
    Call UCheb(X, -0.03950693, TJ, TJ1, Result)
    Call UCheb(X, -0.01231455, TJ, TJ1, Result)
    Call UCheb(X, -0.00397655, TJ, TJ1, Result)
    Call UCheb(X, -0.001538455, TJ, TJ1, Result)
    Call UCheb(X, -0.0007245603, TJ, TJ1, Result)
    Call UCheb(X, -0.0004142647, TJ, TJ1, Result)
    Call UCheb(X, -0.0002831434, TJ, TJ1, Result)
    Call UCheb(X, -0.0002032483, TJ, TJ1, Result)
    Call UCheb(X, -0.0001488405, TJ, TJ1, Result)
    Call UCheb(X, -0.0001156927, TJ, TJ1, Result)
    Call UCheb(X, -0.00007949279, TJ, TJ1, Result)
    Call UCheb(X, -0.000075327, TJ, TJ1, Result)
    UTblN13N13 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 13, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN13N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.525655, TJ, TJ1, Result)
    Call UCheb(X, -4.828341, TJ, TJ1, Result)
    Call UCheb(X, -1.10811, TJ, TJ1, Result)
    Call UCheb(X, -0.1579552, TJ, TJ1, Result)
    Call UCheb(X, -0.03488307, TJ, TJ1, Result)
    Call UCheb(X, -0.01032328, TJ, TJ1, Result)
    Call UCheb(X, -0.002988741, TJ, TJ1, Result)
    Call UCheb(X, -0.0009766394, TJ, TJ1, Result)
    Call UCheb(X, -0.000338895, TJ, TJ1, Result)
    Call UCheb(X, -0.0001338179, TJ, TJ1, Result)
    Call UCheb(X, -0.0000613344, TJ, TJ1, Result)
    Call UCheb(X, -0.00003023518, TJ, TJ1, Result)
    Call UCheb(X, -0.0000111057, TJ, TJ1, Result)
    Call UCheb(X, 0.000004202332, TJ, TJ1, Result)
    Call UCheb(X, 0.00001056132, TJ, TJ1, Result)
    Call UCheb(X, 0.00001536323, TJ, TJ1, Result)
    UTblN13N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 13, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN13N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.513585, TJ, TJ1, Result)
    Call UCheb(X, -4.803952, TJ, TJ1, Result)
    Call UCheb(X, -1.090686, TJ, TJ1, Result)
    Call UCheb(X, -0.149531, TJ, TJ1, Result)
    Call UCheb(X, -0.03160314, TJ, TJ1, Result)
    Call UCheb(X, -0.009073124, TJ, TJ1, Result)
    Call UCheb(X, -0.002480313, TJ, TJ1, Result)
    Call UCheb(X, -0.0007478239, TJ, TJ1, Result)
    Call UCheb(X, -0.0002140914, TJ, TJ1, Result)
    Call UCheb(X, -0.00005311541, TJ, TJ1, Result)
    Call UCheb(X, -0.000002677105, TJ, TJ1, Result)
    Call UCheb(X, 0.00001115464, TJ, TJ1, Result)
    Call UCheb(X, 0.00001578563, TJ, TJ1, Result)
    Call UCheb(X, 0.00002044604, TJ, TJ1, Result)
    Call UCheb(X, 0.00001888939, TJ, TJ1, Result)
    Call UCheb(X, 0.00002395644, TJ, TJ1, Result)
    UTblN13N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 13, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN13N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.455999, TJ, TJ1, Result)
    Call UCheb(X, -4.678434, TJ, TJ1, Result)
    Call UCheb(X, -0.9995491, TJ, TJ1, Result)
    Call UCheb(X, -0.10781, TJ, TJ1, Result)
    Call UCheb(X, -0.0170522, TJ, TJ1, Result)
    Call UCheb(X, -0.004258739, TJ, TJ1, Result)
    Call UCheb(X, -0.0008671526, TJ, TJ1, Result)
    Call UCheb(X, -0.0002185458, TJ, TJ1, Result)
    Call UCheb(X, -0.00005507764, TJ, TJ1, Result)
    Call UCheb(X, -0.00001411446, TJ, TJ1, Result)
    Call UCheb(X, -0.000004044355, TJ, TJ1, Result)
    Call UCheb(X, -0.000001285765, TJ, TJ1, Result)
    Call UCheb(X, -0.0000005345282, TJ, TJ1, Result)
    Call UCheb(X, -0.000000306694, TJ, TJ1, Result)
    Call UCheb(X, -0.0000001962037, TJ, TJ1, Result)
    Call UCheb(X, -0.0000001723644, TJ, TJ1, Result)
    UTblN13N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 13, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN13N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.446787, TJ, TJ1, Result)
    Call UCheb(X, -4.640804, TJ, TJ1, Result)
    Call UCheb(X, -0.9671552, TJ, TJ1, Result)
    Call UCheb(X, -0.0936499, TJ, TJ1, Result)
    Call UCheb(X, -0.01274444, TJ, TJ1, Result)
    Call UCheb(X, -0.00304744, TJ, TJ1, Result)
    Call UCheb(X, -0.0005161439, TJ, TJ1, Result)
    Call UCheb(X, -0.0001171729, TJ, TJ1, Result)
    Call UCheb(X, -0.00002562171, TJ, TJ1, Result)
    Call UCheb(X, -0.000005359762, TJ, TJ1, Result)
    Call UCheb(X, -0.000001275494, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002747635, TJ, TJ1, Result)
    Call UCheb(X, -0.00000005700292, TJ, TJ1, Result)
    Call UCheb(X, -0.000000002565559, TJ, TJ1, Result)
    Call UCheb(X, 0.000000005005396, TJ, TJ1, Result)
    Call UCheb(X, 0.000000003335794, TJ, TJ1, Result)
    UTblN13N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 14, 14)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN14N14(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.510624, TJ, TJ1, Result)
    Call UCheb(X, -4.798584, TJ, TJ1, Result)
    Call UCheb(X, -1.087107, TJ, TJ1, Result)
    Call UCheb(X, -0.1478532, TJ, TJ1, Result)
    Call UCheb(X, -0.0309805, TJ, TJ1, Result)
    Call UCheb(X, -0.008855986, TJ, TJ1, Result)
    Call UCheb(X, -0.002409083, TJ, TJ1, Result)
    Call UCheb(X, -0.0007299536, TJ, TJ1, Result)
    Call UCheb(X, -0.0002176177, TJ, TJ1, Result)
    Call UCheb(X, -0.00006479417, TJ, TJ1, Result)
    Call UCheb(X, -0.00001812761, TJ, TJ1, Result)
    Call UCheb(X, -0.000005225872, TJ, TJ1, Result)
    Call UCheb(X, 0.0000004516521, TJ, TJ1, Result)
    Call UCheb(X, 0.000006730551, TJ, TJ1, Result)
    Call UCheb(X, 0.000009237563, TJ, TJ1, Result)
    Call UCheb(X, 0.0000161182, TJ, TJ1, Result)
    UTblN14N14 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 14, 15)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN14N15(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.498681, TJ, TJ1, Result)
    Call UCheb(X, -4.774668, TJ, TJ1, Result)
    Call UCheb(X, -1.070267, TJ, TJ1, Result)
    Call UCheb(X, -0.1399348, TJ, TJ1, Result)
    Call UCheb(X, -0.02807239, TJ, TJ1, Result)
    Call UCheb(X, -0.007845763, TJ, TJ1, Result)
    Call UCheb(X, -0.002071773, TJ, TJ1, Result)
    Call UCheb(X, -0.0006261698, TJ, TJ1, Result)
    Call UCheb(X, -0.0002011695, TJ, TJ1, Result)
    Call UCheb(X, -0.00007305946, TJ, TJ1, Result)
    Call UCheb(X, -0.00003879295, TJ, TJ1, Result)
    Call UCheb(X, -0.00002999439, TJ, TJ1, Result)
    Call UCheb(X, -0.00002904438, TJ, TJ1, Result)
    Call UCheb(X, -0.00002944986, TJ, TJ1, Result)
    Call UCheb(X, -0.00002373908, TJ, TJ1, Result)
    Call UCheb(X, -0.00002140794, TJ, TJ1, Result)
    UTblN14N15 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 14, 30)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN14N30(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.440378, TJ, TJ1, Result)
    Call UCheb(X, -4.649587, TJ, TJ1, Result)
    Call UCheb(X, -0.9807829, TJ, TJ1, Result)
    Call UCheb(X, -0.09989753, TJ, TJ1, Result)
    Call UCheb(X, -0.01463646, TJ, TJ1, Result)
    Call UCheb(X, -0.00358658, TJ, TJ1, Result)
    Call UCheb(X, -0.0006745917, TJ, TJ1, Result)
    Call UCheb(X, -0.0001635398, TJ, TJ1, Result)
    Call UCheb(X, -0.00003923172, TJ, TJ1, Result)
    Call UCheb(X, -0.000009446699, TJ, TJ1, Result)
    Call UCheb(X, -0.000002613892, TJ, TJ1, Result)
    Call UCheb(X, -0.0000008214073, TJ, TJ1, Result)
    Call UCheb(X, -0.0000003651683, TJ, TJ1, Result)
    Call UCheb(X, -0.0000002272777, TJ, TJ1, Result)
    Call UCheb(X, -0.0000001464988, TJ, TJ1, Result)
    Call UCheb(X, -0.0000001109803, TJ, TJ1, Result)
    UTblN14N30 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, 14, 100)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function UTblN14N100(ByVal S As Double) As Double
    Dim Result As Double
    Dim X As Double
    Dim TJ As Double
    Dim TJ1 As Double
    Result = 0#
    X = MinReal(2# * (S - 0#) / 3.75 - 1#, 1#)
    TJ = 1#
    TJ1 = X
    Call UCheb(X, -4.429701, TJ, TJ1, Result)
    Call UCheb(X, -4.610577, TJ, TJ1, Result)
    Call UCheb(X, -0.9482675, TJ, TJ1, Result)
    Call UCheb(X, -0.0860555, TJ, TJ1, Result)
    Call UCheb(X, -0.01062151, TJ, TJ1, Result)
    Call UCheb(X, -0.002525154, TJ, TJ1, Result)
    Call UCheb(X, -0.0003835983, TJ, TJ1, Result)
    Call UCheb(X, -0.0000841144, TJ, TJ1, Result)
    Call UCheb(X, -0.00001744901, TJ, TJ1, Result)
    Call UCheb(X, -0.00000331885, TJ, TJ1, Result)
    Call UCheb(X, -0.00000076921, TJ, TJ1, Result)
    Call UCheb(X, -0.000000153627, TJ, TJ1, Result)
    Call UCheb(X, -0.00000003705888, TJ, TJ1, Result)
    Call UCheb(X, -0.000000007999599, TJ, TJ1, Result)
    Call UCheb(X, -0.000000002908395, TJ, TJ1, Result)
    Call UCheb(X, 0.000000001546923, TJ, TJ1, Result)
    UTblN14N100 = Result
End Function
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
'Tail(S, N1, N2)
'
''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
Private Function USigma(ByVal S As Double, _
         ByVal N1 As Long, _
         ByVal N2 As Long) As Double
    Dim Result As Double
    Dim F0 As Double
    Dim F1 As Double
    Dim F2 As Double
    Dim F3 As Double
    Dim F4 As Double
    Dim S0 As Double
    Dim S1 As Double
    Dim S2 As Double
    Dim S3 As Double
    Dim S4 As Double
    
    '
    ' N1=5, N2 = 5, 6, 7, ...
    '
    If MinInt(N1, N2) = 5# Then
        If MaxInt(N1, N2) = 5# Then
            Result = UTblN5N5(S)
        End If
        If MaxInt(N1, N2) = 6# Then
            Result = UTblN5N6(S)
        End If
        If MaxInt(N1, N2) = 7# Then
            Result = UTblN5N7(S)
        End If
        If MaxInt(N1, N2) = 8# Then
            Result = UTblN5N8(S)
        End If
        If MaxInt(N1, N2) = 9# Then
            Result = UTblN5N9(S)
        End If
        If MaxInt(N1, N2) = 10# Then
            Result = UTblN5N10(S)
        End If
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN5N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN5N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN5N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN5N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN5N15(S)
        End If
        If MaxInt(N1, N2) = 16# Then
            Result = UTblN5N16(S)
        End If
        If MaxInt(N1, N2) = 17# Then
            Result = UTblN5N17(S)
        End If
        If MaxInt(N1, N2) = 18# Then
            Result = UTblN5N18(S)
        End If
        If MaxInt(N1, N2) = 19# Then
            Result = UTblN5N19(S)
        End If
        If MaxInt(N1, N2) = 20# Then
            Result = UTblN5N20(S)
        End If
        If MaxInt(N1, N2) = 21# Then
            Result = UTblN5N21(S)
        End If
        If MaxInt(N1, N2) = 22# Then
            Result = UTblN5N22(S)
        End If
        If MaxInt(N1, N2) = 23# Then
            Result = UTblN5N23(S)
        End If
        If MaxInt(N1, N2) = 24# Then
            Result = UTblN5N24(S)
        End If
        If MaxInt(N1, N2) = 25# Then
            Result = UTblN5N25(S)
        End If
        If MaxInt(N1, N2) = 26# Then
            Result = UTblN5N26(S)
        End If
        If MaxInt(N1, N2) = 27# Then
            Result = UTblN5N27(S)
        End If
        If MaxInt(N1, N2) = 28# Then
            Result = UTblN5N28(S)
        End If
        If MaxInt(N1, N2) = 29# Then
            Result = UTblN5N29(S)
        End If
        If MaxInt(N1, N2) > 29# Then
            F0 = UTblN5N15(S)
            F1 = UTblN5N30(S)
            F2 = UTblN5N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=6, N2 = 6, 7, 8, ...
    '
    If MinInt(N1, N2) = 6# Then
        If MaxInt(N1, N2) = 6# Then
            Result = UTblN6N6(S)
        End If
        If MaxInt(N1, N2) = 7# Then
            Result = UTblN6N7(S)
        End If
        If MaxInt(N1, N2) = 8# Then
            Result = UTblN6N8(S)
        End If
        If MaxInt(N1, N2) = 9# Then
            Result = UTblN6N9(S)
        End If
        If MaxInt(N1, N2) = 10# Then
            Result = UTblN6N10(S)
        End If
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN6N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN6N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN6N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN6N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN6N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN6N15(S)
            F1 = UTblN6N30(S)
            F2 = UTblN6N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=7, N2 = 7, 8, ...
    '
    If MinInt(N1, N2) = 7# Then
        If MaxInt(N1, N2) = 7# Then
            Result = UTblN7N7(S)
        End If
        If MaxInt(N1, N2) = 8# Then
            Result = UTblN7N8(S)
        End If
        If MaxInt(N1, N2) = 9# Then
            Result = UTblN7N9(S)
        End If
        If MaxInt(N1, N2) = 10# Then
            Result = UTblN7N10(S)
        End If
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN7N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN7N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN7N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN7N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN7N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN7N15(S)
            F1 = UTblN7N30(S)
            F2 = UTblN7N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=8, N2 = 8, 9, 10, ...
    '
    If MinInt(N1, N2) = 8# Then
        If MaxInt(N1, N2) = 8# Then
            Result = UTblN8N8(S)
        End If
        If MaxInt(N1, N2) = 9# Then
            Result = UTblN8N9(S)
        End If
        If MaxInt(N1, N2) = 10# Then
            Result = UTblN8N10(S)
        End If
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN8N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN8N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN8N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN8N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN8N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN8N15(S)
            F1 = UTblN8N30(S)
            F2 = UTblN8N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=9, N2 = 9, 10, ...
    '
    If MinInt(N1, N2) = 9# Then
        If MaxInt(N1, N2) = 9# Then
            Result = UTblN9N9(S)
        End If
        If MaxInt(N1, N2) = 10# Then
            Result = UTblN9N10(S)
        End If
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN9N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN9N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN9N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN9N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN9N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN9N15(S)
            F1 = UTblN9N30(S)
            F2 = UTblN9N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=10, N2 = 10, 11, ...
    '
    If MinInt(N1, N2) = 10# Then
        If MaxInt(N1, N2) = 10# Then
            Result = UTblN10N10(S)
        End If
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN10N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN10N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN10N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN10N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN10N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN10N15(S)
            F1 = UTblN10N30(S)
            F2 = UTblN10N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=11, N2 = 11, 12, ...
    '
    If MinInt(N1, N2) = 11# Then
        If MaxInt(N1, N2) = 11# Then
            Result = UTblN11N11(S)
        End If
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN11N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN11N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN11N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN11N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN11N15(S)
            F1 = UTblN11N30(S)
            F2 = UTblN11N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=12, N2 = 12, 13, ...
    '
    If MinInt(N1, N2) = 12# Then
        If MaxInt(N1, N2) = 12# Then
            Result = UTblN12N12(S)
        End If
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN12N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN12N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN12N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN12N15(S)
            F1 = UTblN12N30(S)
            F2 = UTblN12N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=13, N2 = 13, 14, ...
    '
    If MinInt(N1, N2) = 13# Then
        If MaxInt(N1, N2) = 13# Then
            Result = UTblN13N13(S)
        End If
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN13N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN13N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN13N15(S)
            F1 = UTblN13N30(S)
            F2 = UTblN13N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1=14, N2 = 14, 15, ...
    '
    If MinInt(N1, N2) = 14# Then
        If MaxInt(N1, N2) = 14# Then
            Result = UTblN14N14(S)
        End If
        If MaxInt(N1, N2) = 15# Then
            Result = UTblN14N15(S)
        End If
        If MaxInt(N1, N2) > 15# Then
            F0 = UTblN14N15(S)
            F1 = UTblN14N30(S)
            F2 = UTblN14N100(S)
            Result = UNInterpolate(F0, F1, F2, MaxInt(N1, N2))
        End If
        USigma = Result
        Exit Function
    End If
    
    '
    ' N1 >= 15, N2 >= 15
    '
    If S > 4# Then
        S = 4#
    End If
    If S < 3# Then
        S0 = 0#
        F0 = USigma000(N1, N2)
        S1 = 0.75
        F1 = USigma075(N1, N2)
        S2 = 1.5
        F2 = USigma150(N1, N2)
        S3 = 2.25
        F3 = USigma225(N1, N2)
        S4 = 3#
        F4 = USigma300(N1, N2)
        F1 = ((S - S0) * F1 - (S - S1) * F0) / (S1 - S0)
        F2 = ((S - S0) * F2 - (S - S2) * F0) / (S2 - S0)
        F3 = ((S - S0) * F3 - (S - S3) * F0) / (S3 - S0)
        F4 = ((S - S0) * F4 - (S - S4) * F0) / (S4 - S0)
        F2 = ((S - S1) * F2 - (S - S2) * F1) / (S2 - S1)
        F3 = ((S - S1) * F3 - (S - S3) * F1) / (S3 - S1)
        F4 = ((S - S1) * F4 - (S - S4) * F1) / (S4 - S1)
        F3 = ((S - S2) * F3 - (S - S3) * F2) / (S3 - S2)
        F4 = ((S - S2) * F4 - (S - S4) * F2) / (S4 - S2)
        F4 = ((S - S3) * F4 - (S - S4) * F3) / (S4 - S3)
        Result = F4
    Else
        S0 = 3#
        F0 = USigma300(N1, N2)
        S1 = 3.333333
        F1 = USigma333(N1, N2)
        S2 = 3.666667
        F2 = USigma367(N1, N2)
        S3 = 4#
        F3 = USigma400(N1, N2)
        F1 = ((S - S0) * F1 - (S - S1) * F0) / (S1 - S0)
        F2 = ((S - S0) * F2 - (S - S2) * F0) / (S2 - S0)
        F3 = ((S - S0) * F3 - (S - S3) * F0) / (S3 - S0)
        F2 = ((S - S1) * F2 - (S - S2) * F1) / (S2 - S1)
        F3 = ((S - S1) * F3 - (S - S3) * F1) / (S3 - S1)
        F3 = ((S - S2) * F3 - (S - S3) * F2) / (S3 - S2)
        Result = F3
    End If
    USigma = Result
End Function
